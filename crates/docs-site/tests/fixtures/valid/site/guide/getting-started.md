# Getting Started

Fixture Markdown for `docs-site` E2E tests (issue #869/#870).

```rust
fn main() {
    println!("hello");
}
```

Some **bold**, *em*, and ***bold+em*** text, plus a [link](/guide/intro/).
