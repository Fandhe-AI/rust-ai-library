# CLAUDE.md

## Overview

Rust 製 AI/ML ライブラリの実装リポジトリ（v2）。Burn 依存を排した**完全自作コア**（テンソル・autodiff・演算グラフ／カーネル融合機構・計算カーネル・バックエンド抽象層）で実装する。仕様の正本は [Fandhe-AI/fandhe-ai-spec](https://github.com/Fandhe-AI/fandhe-ai-spec)（`docs/spec` submodule）にあり、本リポでは編集しない。本リポジトリ自体は **public**（#457 Phase 1〜3 完了）で、CI は GitHub ホステッド `ubuntu-latest` 既定へ移行済み（self-hosted への逆戻りは `runner-policy` ジョブ〈#472〉が fail-closed で検知。詳細 → `.claude/rules/ci.md`）。仕様 submodule（`docs/spec`）と旧実装（v1）は private を維持する（README「位置づけ」節）。

- 想定クレート 10 個: `tensor-core`・`autodiff`・`backend-cpu`・`backend-cuda`・`backend-metal`・`onnx-interop`・`guardrail`・`self-repair`・`bench-harness`・`facade`（TASK-9.3・イシュー #410 で新設した composition root に、TASK-9.4・イシュー #411 で `autodiff::compat` から compat 公開面〈`compat::array`・`compat::Sequential`〉を移設済み。`facade` が唯一のサポートされる公開 API 面であり `tensor-core`・`autodiff`・`backend-*` は内部クレート。`docs/compat-api-scope.md` §0）に加え、GitHub Pages 公開ツリー（#865 Phase 1）向けの開発者・CI 専用 SSG クレート `docs-site`（11 個目・`publish = false`。イシュー #868/#869。本体ライブラリの公開 API とは無関係で外部依存ゼロ）。上記の名称はディレクトリ名（`crates/<name>`）であり変更しない。crates.io 公開対象 6 クレート（`facade`・`tensor-core`・`autodiff`・`backend-cpu`・`backend-cuda`・`backend-metal`）は `[package] name` を `fandhe-ai` prefix 付き公開名（`fandhe-ai`・`fandhe-ai-tensor-core`・`fandhe-ai-autodiff`・`fandhe-ai-backend-cpu`・`fandhe-ai-backend-cuda`・`fandhe-ai-backend-metal`）へ rename 済み（イシュー #877/#879・#878 でユーザー承認済み。`docs/crates-io-naming-decision.md`）。`onnx-interop`・`guardrail`・`self-repair`・`bench-harness`・`docs-site` は非公開のため対象外
- crates.io への公開は一括リリース `.github/workflows/release-all.yml`（workflow_dispatch 1 回・environment `crates-io-release` 承認 1 回で公開 6 クレートを依存順に publish）を基本とし、単一クレートの再実行・障害復旧には `.github/workflows/release.yml` を使う（いずれも `CARGO_REGISTRY_TOKEN`〈org secret〉・fail-closed ガード群。手順・版数運用の正は `docs/crates-io-publishing-order.md`）。初回公開（v0.3.0・6 クレート）は 2026-08-23 に完了済み（crates.io・docs.rs 反映確認済み。同 doc §10 追補）。v0.4.0 は 2026-08-29 に `release-all.yml` で公開済み（同 doc §10 追補）。v0.5.0 は 2026-08-31 に同ワークフローで公開済み（同 doc §10 追補。framework-compare の承認ピンをイシュー #1011 で `=0.5.0` へ更新済み）。v0.6.0 は 2026-09-02 に同ワークフロー（run 33503500987）で公開済み（同 doc §10 追補。framework-compare の承認ピンを `=0.6.0` へ更新済み）。v0.7.0 は 2026-09-06 に同ワークフローで公開済み（同 doc §10 追補。イシュー #1185 に対するユーザー指示に基づき framework-compare の承認ピンを `=0.7.0` へ更新済み）。v0.8.0 は 2026-09-09 に同ワークフロー（run 34417008617）で公開済み（同 doc §10 追補。framework-compare の承認ピンを #1487 で `=0.8.0` へ更新済み）
- 依存は許容 8 区分のみ・`=x.y.z` 完全固定（`.claude/rules/deps-policy.md`）。禁止リスト（`burn` 系・`cubecl`・`candle`・`tch`・`ndarray`）は CI で機械検査
- バックエンド切替は feature フラグなしの cfg ベース（PoC-v2-5 実証構成）
- 現状 M0 完了（TASK-1 通し完了: workspace Cargo.toml・9 クレート雛形・本体 workspace 直接依存の許容 8 区分〈ベンチ比較対象の第 9 区分は `scripts/bench/oss-gemm-compare/` 限定。計 9 区分の正は `.claude/rules/deps-policy.md`〉の =x.y.z 完全固定・依存禁止検査・deny.toml・license-matrix.md）。crates.io 初回公開 v0.3.0（2026-08-23）・GitHub Pages 公開済み（`crates/docs-site` + `site/` + `.github/workflows/docs-site.yml`。ヘッダーのホバーメニュー・セクション連動サイドバーは #909）。CI・Makefile の cargo 系チェック（fmt / clippy / test / deny / deps-forbidden）は全て有効化済み

## Repository Structure

```
fandhe-ai/
├── CLAUDE.md                # 本ファイル
├── README.md                # 開発環境構築・実装方針の要点
├── LICENSE-APACHE           # Apache License 2.0 全文（MIT/Apache-2.0 デュアルライセンス。#462）
├── LICENSE-MIT              # MIT ライセンス本文（同上）
├── Makefile                 # make setup / ci / docker-* タスクランナー
├── lefthook.yml             # git hooks（rustfmt-check・secrets-guard・commit-msg・pre-push）
├── .editorconfig            # インデント・改行規約
├── Dockerfile / compose.yaml # 環境非依存の開発コンテナ（CPU バックエンドのみ）
├── skills-lock.json         # 導入スキルのハッシュ管理（npx skills）
├── Cargo.toml                # workspace 定義（本体 10 クレート + docs-site〈開発ツール〉・許容依存 8 区分を =x.y.z 固定）
├── Cargo.lock                # 依存解決の完全固定（deps-policy.md）
├── rust-toolchain.toml       # toolchain 単一真実源（stable + rustfmt/clippy。rust-base-ci 前提。#325）
├── deny.toml                 # cargo-deny 設定（licenses 許可リスト・sources = crates.io 限定〈TASK-1.3〉+ advisories / bans〈#353〉）
├── guardrail.toml             # guardrail 判定閾値の確定設定（TASK-4.3c・#117。default プリセット）
├── crates/                  # tensor-core・autodiff・backend-cpu・backend-cuda・backend-metal・
│                             # onnx-interop・guardrail・self-repair・bench-harness・facade（composition root・compat 公開面）・
│                             # docs-site（GitHub Pages 公開ツリー向け SSG。開発者・CI 専用。#868/#869）
├── scripts/
│   ├── check-forbidden-deps.sh # 依存禁止リストの検査ロジック（ci.yml・Makefile 共用。TASK-1.2）
│   ├── check-workflow-runner-policy.sh # self-hosted runner 逆戻り防止の fail-closed 契約検査の呼び出し面（ci.yml・Makefile 共用。#472）
│   ├── check-workflow-runner-policy.py # 同検査の本体（python3 標準ライブラリのみの自前 YAML サブセットパーサー方式。追加依存なしで表記トリック迂回を遮断。#472・PR #626）
│   ├── run-verification-gates.sh # AI 自律メンテナンス検証 4 ゲート（build/test/clippy/bench）の実行ロジック（ci.yml・Makefile 共用。TASK-6.1c）
│   ├── run-guardrail-regression.sh # guardrail 2 層検証ロジック（ci.yml・schedule 共用。TASK-6.1a）
│   ├── report-guardrail-schedule-result.sh # schedule 定期実行失敗時の Issue 起票・復旧クローズ（TASK-6.1b）
│   ├── report-clippy-nocache-schedule-result.sh # キャッシュなしフルビルド clippy 定期検証の失敗時 Issue 起票・復旧クローズ（イシュー #918）
│   ├── testdata/             # 上記の self-test 用固定 fixture
│   └── bench/
│       ├── oss-gemm-compare/ # CPU GEMM OSS 直接比較ハーネス（本体 workspace 外の独立 Cargo パッケージ。matrixmultiply・gemm crate。イシュー #755）
│       ├── gemm_bench_torch_mps_f16.py／gemm_bench_torch_mps_f32.py # PyTorch MPS 参照計測
│       ├── gemm_bench_torch_cpu_f32.py # PyTorch CPU f32 GEMM 参照計測（イシュー #1141）
│       └── gemm_bench_mlx_f32.py # MLX f32 GEMM 計測（イシュー #755）
├── .github/workflows/
│   ├── ci.yml               # rust-ci（Fandhe-AI/actions rust-base-ci 呼び出し: fmt / clippy / test / deny。#325）+ 固有ジョブ（build / build-no-cuda-toolkit / deps-forbidden / runner-policy / guardrail-regression / verification-gates）+ ci-complete
│   ├── codex-review.yml     # Codex PR 自動レビュー wrapper（Fandhe-AI/actions codex-review を SHA 固定呼び出し。#326。public 構成〈post-feedback-runner-label: ubuntu-latest〉へ切替済み。#469）
│   ├── verification-gate-bench.yml # bench ゲート（schedule／workflow_dispatch。TASK-6.1c）
│   ├── guardrail-regression-schedule.yml # guardrail 2 層検証の schedule 定期実行・失敗時 Issue 可視化（TASK-6.1b）
│   ├── clippy-nocache-schedule.yml # キャッシュなしフルビルド clippy の定期検証・失敗時 Issue 可視化（イシュー #918）
│   ├── release.yml          # crates.io publish（workflow_dispatch + `CARGO_REGISTRY_TOKEN`・environment `crates-io-release` 承認ゲート。#884。手順は `docs/crates-io-publishing-order.md` §9〜11・`.claude/rules/ci.md` release.yml 節）
│   ├── update-external.yml  # docs/spec・.claude/skills の自動追従
│   └── docs-site.yml        # GitHub Pages ビルド・デプロイ（Fandhe-AI/actions `pages-deploy.yml` 呼び出し）
├── site/                    # GitHub Pages 公開原稿（`nav.toml` + Markdown。#873/#874/#875。`docs/spec` の内容は含めない）
├── .claude/
│   ├── agents/              # research / implement / testing / quality / docs
│   ├── rules/               # 委譲・コーディング・依存・CI・セキュリティ等の規約
│   ├── skills/              # npx skills add で導入（skills-lock.json 管理）
│   ├── workflows/           # implement-issue-tree.js（skills への相対 symlink）
│   └── settings.json        # SessionStart / PostToolUse hooks
└── docs/
    ├── autodiff-custom-function-decision.md # custom autograd Function（ユーザー定義 Op）プラグイン機構の設計判断（`Op` enum への trait object variant〈案 B〉・固定集合の勾配差し替え Op〈案 C〉・合成のみ〈案 A〉の比較、REQ-12 との整合〈`BackendOps` 非露出限定で抵触なし〉、前提イシュー〈#1612／#1593／#1634〉完了までは現時点非対応と明文化する段階 0 を確定。承認事項は §10。#1623）
    ├── autodiff-higher-order-grad-decision.md # 高階微分（grad of grad）の設計判断（VJP の VJP・テープ再設計の要否検討。同一テープ方式は backward_impl の不変借用と衝突・子テープ方式〈create_graph〉を主案・forward-over-reverse〈HVP 限定〉を代替として比較。段階 0〈現時点では非対応と明文化〉を確定し実装は前提イシュー〈#1593／#1597／#1599／#1601／#1612〉完了後の別イシューへ引き継ぐ。承認事項は §10。#1622）
    ├── autodiff-linalg-design.md # 線形代数（inv／solve／det／qr／cholesky／svd）・matrix_norm の設計（REQ-9 2026-09-12 追記の Tier 2 対応・多出力ノード〈QrQ/QrR・SvdU/SvdS/SvdVh〉設計・数値契約〈f64 内部計算・符号／ゲージ規約〉・VJP 方式・eval と CPU 実装の意図的複製・テスト構成。CPU 実装先行・GPU は Unsupported フォールバック。イシュー #1621・親 #1573）
    ├── autodiff-nograd-leaf-dinput-skip-decision.md # 非学習葉（活性化入力 x 等）への d_input 伝播スキップの設計判断（`requires_grad` 前方伝播案の採用・`Gradients::get` 契約整理・多層依存整理・起票草案。#1151 起票案 I・#1219）
    ├── autodiff-rnn-cell-tape-design.md # RNN／LSTM／GRU のセル演算（専用 Op・payload 保持ゲート値）・LSTM 2 出力（h_t／c_t）の 2 ノード表現・時系列ループ（unrolled）の tape 設計判断（#1646）。**#1647 で内部クレート `fandhe_ai_autodiff::nn::rnn` として実装済み**（3 バックエンド〈CPU・CUDA・Metal〉数値一致・数値微分突合・BPTT・多層スタック検証済み。Metal は実機実測完了・CUDA は本エージェント実行環境に実機なしのため未実測明記。§8「実装記録」参照）。facade 公開面拡張は `docs/compat-api-scope.md` §5 手続き〈#1591。CLOSED〉のうちユーザー承認が未取得のため引き続き実装不可
    ├── autodiff-view-recompute-decision.md # view 系ノード（reshape / transpose）の再計算方式化の設計（push_view／resolve_view による中間バッファ非確保・融合境界化・実測記録。#1043 ツリー・#1047）。§5 追記: permute／broadcast_to の Var 化は #1597 で実装済み・narrow の Var 化は #1598 で実装済み（`split`／`split_with_sizes`／`chunk` は narrow への委譲。#1599 は narrow を対象外とし where／gather／scatter に集中）
    ├── backend-abstraction-amd-readiness-decision.md # 将来の AMD（ROCm/HIP）追加に備えた抽象境界の設計記録（warp 幅の実行時パラメータ化・シャッフルのマスク差異吸収・起動種別〈通常／persistent／cooperative〉の区別・stream 優先 API。32 固定箇所の棚卸し。コード変更なし。#1340）
    ├── backend-cpu-gb10-affinity-design.md # GB10（DGX Spark GB10）小形状 GEMM 向け大コア OS affinity 自機判定の設計（`crate::gb10_affinity`。`crate::thread_limit`〈スレッド数制限のみ・#1364 REJECT 確定〉とは独立の別系統機構。cpufreq／MIDR_EL1 partnum クロスバリデーション検出・`sched_setaffinity` unsafe FFI・GEMM 専用 rayon ThreadPool・`m*n*k` 小形状限定ルーティング・bit 完全一致契約。既定 OFF。性能 A/B は GB10 実機実測を伴う後続セッションへ引き継ぎ。`docs/perf/cpu-gemm-gb10-affinity-ab.md` に記入欄。イシュー #1576）
    ├── backend-cuda-async-execution-design.md # CUDA 非同期実行モデルの同期契約（ストリーム順序・エラー伝播・D2H 境界・poison/invalidate 状態機械。#1011 ツリー・#1012。§12d で #1349 の CUDA Graph step capture opt-in 実装記録を追記・§14 で managed 配置〈#1352〉の同期契約差分を追記）
    ├── backend-cuda-graph-step-capture-design.md # 学習 step の update 区間（`sgd_step_device_tracked`）を対象とした CUDA Graph capture／instantiate／launch 経路の opt-in・既定 OFF 実装（`StreamKind`・capture 状態機械・`backend-cuda::graph`・`BackendOps::captured_segment_key`／`run_captured_segment`・`DeviceParamStore::step` 結線）。step 全体 capture は既存データパスの制約により不可能と判明しスコープを update 区間へ限定した経緯・exec update／forward・backward capture のスコープ外整理（イシュー #1349・親 #1348・ルート #1341 → #1269）。#1350 で GB10 実機実測完了（§5）: capture 機構は成立（bit 同一 2662 行・launch カウンタ assert green）・性能実測（A/B）・採否確定は `docs/perf/train-step-phase-breakdown.md` §16 を参照（既定 OFF 維持・中立）。#1480 で各 step の重み勾配（`Tape::param_grads_to_host`。イシュー #1479 で追加した公開 API）を bit 同一性比較対象へ追加し（§5.1）、GB10 実機で 4782 行すべて bit 同一を確認（loss・入力勾配・パラメータに加え重み勾配も capture 経路・非 capture 経路で一致することを直接検証）
    ├── backend-cuda-managed-placement-decision.md # CUDA managed 配置（`cuMemAllocManaged`）を `DeviceBuffer` の opt-in 配置として実装する設計判断（host-registered／`cuMemAdvise`／`prefetch` 不採用理由・既定 OFF・fail-closed・出力 bit 同一契約・スコープ外事項。#1352。実機実測完了（GB10・#1353）: 契約テスト 20 件全 pass・非後退確認。性能実測・既定化可否判断〈REJECT〉は `docs/perf/cuda-managed-placement-ab.md` を参照）
    ├── backend-cuda-pool-allocator-decision.md # CUDA サイズクラス別プールアロケータ（自作 SizeClassPool<H> 案 B・driver プール〈cuMemPoolTrimTo〉併用）の採用判断・alloc_uninit 適用確認範囲・実測記入欄（#1018 ツリー・#1020）
    ├── backend-cuda-ptx-embedding-decision.md # CUDA GEMM ビルド時 PTX 事前埋め込み（candle 方式）不採用判断・JIT キャッシュ結線範囲の更新（#1024）
    ├── backend-dtype-dispatch-design.md # dtype 別 dispatch（`BackendOps` の dtype 多重化）方式の設計記録。capability accessor（`typed_ops_f64`／`typed_ops_f16`／`typed_ops_bf16`）＋新設 `TypedOps<T: Scalar>` trait を採用（既定 `None`・非破壊拡張・object safety 維持・f32 経路 bit 同一契約）。`dispatch::DType` は非拡張のまま新設 `ScalarDType`（`#[non_exhaustive]`）で dtype を表現。tolerance／baseline は変更なし。設計のみ・コード変更なし。承認事項（trait 拡張・新規公開型）は §7 に列挙し未承認のまま実装（#1649 CPU → #1650 CUDA → #1651 Metal）へ引き継ぐ（イシュー #1648・親 #1626）
    ├── backend-metal-aligned-load-decision.md # Metal GEMM アラインメント特化ロード分岐（align_M/N/K function constant 方式）不採用判断（#752 保留 → #808 格下げ）
    ├── backend-metal-async-copy-decision.md # Metal 非公式 simdgroup_async_copy 系 AIR intrinsic 不採用の決定記録（#546）
    ├── backend-metal-command-batching-design.md # コマンドバッファ・エンコーダ共有と同期境界（waitUntilCompleted）の設計・CUDA 側契約との対応（#1015 ツリー・#1016。§7 に #1017 実装記録追記。#1550 で §7 に dispatch 回数テストの切り分け記録を追記。#1555 で §7.2 追記：`d_weight` GEMM 同期境界最適化・カウンタ 11/10/10 → 11/9/9。#1562 で §7.3 追記：`d_input` 経路（`gemm_resident_lhs`）の同期境界の切り分け・定量化。コード事実整理・回収案 4 候補の比較表・診断テスト 2 種〈`mnist_scale_train_reuse_metal_backward_dinput_phase`／`resident_lhs_dinput_phase_bench`〉整備・M4 Max 実機実測記入欄〈未実測〉。親イシュー #1557・#1561 の最初の子イシュー）。#1565 で §10 に bias 勾配 resident 化（案 A′）／upload_into 同期遅延（案 B）の比較・採用決定（案 A′ 採用・実装は #1566 へ引き継ぎ。コード変更なし）を追記。#1563 で §7.4 に層内合流（案 (a′)。encode-only の d_weight を d_input の同期点より前へ移し合流させる。bit 同一が構造的に成立）の実装記録を追記（カウンタ 11/9/9 → 11/8/8。d_input 自身の同期境界 2 件は回収しないと結論・(a)/(b)/(c) の各理由・#1564／#1566 との関係を整理。M4 Max 実機実測記入欄〈未実測〉）。#1566 で §10.7 に実装記録（`gemm_fp32_strict_into_with_bias_reduce_tracked`・`encode_weight_and_bias_grad_with_offsets`・weight tying 時の bias 勾配 silent 上書きの是正）を追記。#1659（PR #1659→#1665→#1666 取り込み）で §10.8〜§10.12 に bias 縮約の数値方式統一（f64 相当。§10.8）・`Op::LinearResident`／`Op::LinearAct` フォールバックの f64 ヘルパ統一（§10.9）・Metal 実装形の精密化（単純 Neumaier→scale 方式併用〈§10.10〉→ scale を 2 のべき乗へ限定〈§10.11〉）・`Op::Add`（`nn::Linear` 既定 forward 経路）への横展開（§10.12）を追記。§10.13 の判定契約 2 層構造化（Tier A/B）は §10.14 で撤回し、Metal カーネルを binary64 逐次加算の 64bit 整数ソフトウェアエミュレーション（`crates/backend-metal/src/soft_f64.rs` が逐語モデル）へ置換してホスト `f64` 参照実装と bit 完全一致させた（bias 勾配も weight と同じ bit 一致契約。tolerance・REQ-2 適用範囲は不変・契約 PR #1666 は superseded）
    ├── backend-metal-mlx-classic-nax-decision.md # MLX classic 経路と CANDIDATES の構成対比・NAX 経路不採用判断（#549）
    ├── backend-metal-mpp-tensor-decision.md # Metal 4 `tensor<>`＋Metal Performance Primitives（MPP）行列積の可用性・バインディング到達性（Route C）・純カーネル時間 A/B・「完全自作コア」との整合のユーザー判断事項整理（採否の結論は出さない。M4 Max 実機実測。#1326）
    ├── backend-metal-morton-mapping-decision.md # 標準 simdgroup_matrix API 下での Morton 順レーン→要素マッピング適用不可の判断（#544）
    ├── backend-metal-splitk-decision.md # split-K ディスパッチ分岐の MLX 選択条件対比・採否判断（#810）。#1308 で M4 Max 実機実測を完了し「採用検討推奨」を確定記録（対象 9 点全点で劣化率中央値 0.2265〜0.5166・5/5 run 一貫。split-K 実装自体は別 issue へ切り出し提案・本 PR ではコード変更なし）。#1474 で opt-in 実装完了（`dispatch_auto` へ未結線）を追記。実装記録は `docs/perf/metal-gemm-splitk-two-pass.md`。#1475 で A/B 実測（暫定 ADOPT・3/5 run）を §3 追記（`docs/perf/metal-gemm-splitk-ab.md`）。#1476 で本番結線可否を §4 に確定記録（**結線しない**。性能判定 undetermined・数値契約未承認の 2 ブロッカー。`select_for_device`／`dispatch_auto`／`MetalBackendOps::gemm` は不変・`SPLIT_K_NUMERIC_CONTRACT_APPROVED=false` 維持）。#1513 で §4 に追記: 数値契約ブロッカーは #1511 承認・`SPLIT_K_NUMERIC_CONTRACT_APPROVED=true` 切替で解消済み（ゲート解除自体は自動判定入口 `dispatch_split_k_strided_prepared` 限定・`select_for_device`／`dispatch_auto` への本番結線は不変のまま #1516 へ引き継ぎ）。残る性能ブロッカーにより「結線しない」の結論自体は不変。#1515 で §4「再開条件・結線案メモ」を追記: #1475 §7 の残 run（run4/run5）を #1515 の新規 5 run（`docs/perf/metal-gemm-splitk-ab.md` §10。専有ゲートは受け入れ条件にしない・record_only 運用）で確定する旨へ差し替え（本 PR 時点では未実測）。#1538（#1515 の実測）で 2026-09-11 に M4 Max 実機 5 run を record_only・共有負荷下で完走し ADOPT を正式確定（`docs/perf/metal-gemm-splitk-ab.md` §10.4）。#1516 で §5「本番結線（#1516）」を新設（旧 §5「参照」は §6 へ繰り下げ）: `crate::tile::GemmRoute`／`select_route_for_device`（新設。`select_for_device` 自体は crates.io 公開互換のため不変）・`MetalGemm::split_k_auto_enabled`／`new_with_split_k_auto`／診断専用 `dispatch_auto_with_route`（`internal-diagnostics` feature 限定）を追加し、`dispatch_auto` を定数ゲート `SPLIT_K_DISPATCH_AUTO_PRODUCTION_ENABLED`（**既定 `false`**。#1515 が ADOPT 未確定のため）付きで split-K 2 パス経路へ結線。ゲート OFF（明示 opt-out）では結線前と bit 同一（`tests/gemm_splitk_auto_wiring.rs` の実機 `#[ignore]` テスト・ゲート定数のドリフト検出テストで機構的に担保。実機〈Apple Silicon〉実測は Mac セッションへ申し送り）。#1518 で §5 に記述整合の棚卸しを追記（`.rs` doc comment のフォローアップは切り出し先未起票のまま）。§5「切替判断の記録」に 2026-09-11 の 4 セル後退・ゲート `false` 維持を記録（#1515 ADOPT・#1516 実装済み未マージ・#1517 後退で規則どおり差し戻し）。その後 #1516 のマージで §5「ユーザー判断による本番結線」を追記: 後退 4 セルが split-K 非到達のノイズ帯であること・保守性（本番非到達経路の解消）を根拠に **`SPLIT_K_DISPATCH_AUTO_PRODUCTION_ENABLED = true` へ切替**（#1517 の判定記録は不変。ゲート定数の撤去は別 PR）。#1545 で §5「実行時トグル」を追記: `facade::set_metal_split_k_gemm_enabled`／`metal_split_k_gemm_enabled`（`cfg(target_os = "macos")`。既定 `true`・`false` で classic 経路固定＝結線前と bit 同一。`backend-metal::split_k_runtime` の `AtomicBool` を `dispatch_auto` が毎呼び出しで読む CUDA `precision.rs` 同型の設計）と bench-fandhe `--metal-split-k <on|off>`（feature `metal-split-k-toggle`。ピン未収録 API のため path patch 限定）を追加し `run_ab_splitk_metal.sh` を同一バイナリ runtime 切替へ更新。#1547 で `SPLIT_K_DISPATCH_AUTO_PRODUCTION_ENABLED` 定数とドリフト検出テストを撤去し、split-K の切替を per-instance フィールド（`MetalGemm::new` は `true` 固定）と実行時トグルへ一本化（`run_ab_splitk_metal.sh` の差分ガードは `split_k_runtime::SPLIT_K_RUNTIME_ENABLED` の既定値宣言行検証へ付け替え）。PR #1553 の codex-review 指摘（本番コンストラクタの既定値を検証する Linux 実行可能な契約テストの喪失・差分ガードが実行時トグル初期値しか見ておらず本番コンストラクタ側の有効化状態を検証していなかった点）を受け、既定値の単一情報源 `split_k_runtime::SPLIT_K_DEFAULT_ENABLED` を新設して両者を seed する形へ是正し、差分ガードを 4 点検証（既定値定数宣言・実行時トグル初期値の参照・`gemm.rs` 7 箇所の参照数一致・旧コンパイル時ゲートの残存検出）へ強化した（`docs/backend-metal-splitk-decision.md` §5「定数ゲートの撤去（#1547）」追記参照）
    ├── backend-metal-splitk-parity-judgment-decision.md # Metal f32 split-K 経路の受け入れ判定方式（実測ベースライン非後退方式の適用拡張）の決定記録（**2026-09-10 ユーザー承認済み・§7**。全 11 形状一律 baseline・tolerance 定数不変・spec 提案は Fandhe-AI/fandhe-ai-spec#65 として起票済み）。候補比較（厳密ゼロ fail 維持／baseline 非後退方式／候補 A′ 救済項）・推奨（baseline 非後退方式・全 11 形状一律適用）・spec 起票用本文案を整理（draft 作成時〈PR #1523〉は未承認のまま記録し、承認記録は PR #1525 で §7 へ追記。イシュー #1511）。**#1512 で `crates/backend-metal/tests/gemm_splitk_parity.rs` を承認済み baseline 方式へ再切替済み**（Linux 実行可能な契約テスト `tests/splitk_parity_baseline_contract.rs` を新設。実機〈Apple M4 Max〉での全形状 pass 確認は未実施のまま `docs/perf/metal-gemm-splitk-two-pass.md` §5.8 に記入欄を残す）
    ├── backend-metal-transpose-collapse-design.md # 転置パターン別 strided GEMM 入口（GemmStrides）・先頭次元 collapse の設計・実機実測記入欄（#1029 ツリー・#1040）
    ├── backend-metal-wgpu-decision.md  # Metal バックエンド実装方式（wgpu 非採用）の決定記録
    ├── backend-switching-design.md     # cfg ベースバックエンド切替の設計
    ├── candle-parity-tolerance-contract-decision.md # tolerance 契約変更（N=2048 candle 比 GEMM parity 判定不能の解消）の決定記録 draft。候補 A（スケール付き絶対誤差）／B（ULP ベース）の比較表・推奨 A-1〈係数 c=0.5／1.0 の 2 案。未承認〉・fandhe-ai 側 0 fail 不変の根拠・spec 提案要否（#1240 対応）・Phase 2 反映範囲を整理。tolerance 定数・判定式・`BASELINES`・`docs/spec/` は不変のまま。採否・係数値・適用スコープはイシュー #1241 でユーザー承認（#1237／#1238 引き継ぎ・イシュー #1239。**#1241 で確定版化（2026-09-08 ユーザー承認: 採否 承認・A-1 `c=0.5`・ハーネス限定〈本体 `compare`／`assert_parity`／`ParityBaseline` 不変〉・spec 起票 可〈(b) 形式〉・既存定数 維持。§8 に承認記録・§9 に #1254／#1256 の再スコープ要注記）**）
    ├── cpu-gemm-2d-dynamic-partition-design.md # `gemm_blis_parallel` の (mc, nc) 2D タイル動的分配（rayon work stealing）設計・bit 完全一致条件・C 列分割の unsafe 非導入主案／raw pointer 代替案（#1338 承認済み）・job 粒度算出・`GemmDriverVariant::TwoDDynamic` の A/B 統合方針・#1305（DGX N=1024 の異種コア由来非単調性）を受けた採用ゲート・中止条件を確定（設計記録のみ・コード変更なし。#1307）。#1312 で両実機実測を追記: DGX は全形状で `RowPanel` を上回ったが M4 Max 専有ゲート不通過のため判定 undetermined（`docs/perf/cpu-gemm-2d-dynamic-partition-ab.md`）。§12 で #1313（M4 Max 専有ゲート付き Phase 0 再計測・ADOPT 確定・本番結線）の実施内容を追記
    ├── cpu-gemm-b-packing-sharing-decision.md # B パネル packing のスレッド間共有化の設計検討・適用可否判断（#565）
    ├── cpu-gemm-prefetch-decision.md   # aarch64 プリフェッチ intrinsics 到達可能性調査・E-7 保留判断→原則不要へ格下げ（#489・#751）。2026-09-08 追補（#1319）でマイクロカーネル残差感度（`R_dram = streamed_dram/l1_resident` の 5 run 中央値の比）を両実機実測し「根拠あり」へ再転換（DGX Spark GB10 が無 pin・大コア pin の両条件で単独成立: 無 pin single 0.4550／multi 0.2781、大コア pin single 0.4543／multi 0.1854、いずれも 5/5 run が事前宣言ゲート充足。M4 Max は再計測時の高負荷〈load average 約 40〉によりノイズが大きく単独では判定不能）・codex-review 指摘 2 件（全コア同時計測区間の同期・`aggregate.py` 代表値算出式）を是正し再計測・`unsafe asm!` は未実装のまま承認依頼の要点を記録（`docs/perf/logs/cpu-gemm-prefetch-bandwidth-1319/`）
    ├── cpu-mse-backward-sequential-threshold.md # `mse_loss_backward` の要素数しきい値による逐次フォールバック（bit 同一契約）の設計・事前登録規則・Phase 0（M4 Max・GB10 両実機 5 プロセス起動。候補 `T=1<<18`）・Phase 1（framework-compare train A/B。M4 Max 非後退・GB10 reuse セル後退 `ratio=1.0167`）の実測記録。事前登録規則により GB10 1 件の超過で **REJECT** 確定・`MSE_BACKWARD_PARALLEL_MIN_ELEMS=0`（常に並列＝変更前と bit 同一）で出荷（機構は維持。イシュー #1578）
    ├── elementwise-vjp-backend-ops.md # elementwise VJP（`Op::Mul`／`Op::Exp`／`Tanh`／`Sigmoid` の乗算・`backward.rs::accumulate` の fan-out 勾配合算）を `BackendOps`（forward と同じ CPU 並列／CUDA／Metal カーネル）経由へ切り替えるゲート `ELEMENTWISE_VJP_VIA_BACKEND_OPS` の設計・事前登録規則・正しさ検証（勾配 bit 完全一致・単体テスト 12 件）・M4 Max（cpu／metal）・GB10（cuda）3 バックエンド実測記録。18 セル中いずれのバックエンドも `ratio > 1.00` セルを含み **3 バックエンドとも REJECT** 確定・`ELEMENTWISE_VJP_VIA_BACKEND_OPS=false`（変更前と bit 同一・機構は維持）で出荷。付随是正として `backend-cpu::elementwise::binary_elementwise` の non-contiguous 経路を stride 読み（`ElementwiseReadOperand`。#1577 と同型）化（性能実測対象外・bit 同一）。イシュー #1583
    ├── cpu-matmul-fixed-cost-design.md # facade 経由 CPU matmul（tape 経路）の固定費（contiguous コピー・tape 登録・出力アロケーション）削減設計・bit 一致契約への影響・#1299/#1301 向け受入基準（#1294）。§10 で #1299 の実装記録（案 1a を `zeroed_output` として実装・本番既定は `usize::MAX` で無効化〈M4 Max スモークで N=2048 が約 29% 後退〉）に加え、#1301 で両実機 on/off 実測の結果（DGX N=2048 alloc_c 約 48% 削減・両実機非後退）を確認していったん `2 << 20` へ有効化したが、PR #1448 の codex-review 指摘（事前宣言した candle 比の非後退規則を計測後に緩和した基準だけで満たすとした点）を受け `usize::MAX` へ差し戻し・独立の再計測（同 doc §20.6）へ引き継いだことを追記。#1481 で独立再計測を実施し verdict=REJECT を確定（`docs/perf/cpu-gemm-candle-gate-remeasurement.md` §20.7・本番既定 `usize::MAX` 維持）。#1482 で `usize::MAX` を確定既定化（§10 追記）
    ├── compat-api-scope.md # compat API 層の対象範囲（§0 サポート境界〈`facade` 唯一の公開 API 面〉・§1 対象範囲・§2 対象外・§5 範囲拡張手続き）。#1591 で spec REQ-9 の 2026-09-12 改定（Tier 1／Tier 2 網羅・spec PR #69）へ追従し、§1 を実装済み（1.1）／Tier 1（1.2・Phase 2 各 issue 対応）／Tier 2（1.3・Phase 3 各 issue 対応）の 3 層へ再編・§2 の Softmax／GELU／Conv／Embedding／`fit()`／`compile()` 除外を撤去・§5 に Tier 2 量子化（#1627）／DDP（#1628）の除外事項従属ゲートを追記。#1594 で Tier 1「softmax／log_softmax」行を実装済みへ更新（`Var::softmax`／`log_softmax`・`nn::activation::Softmax`／`LogSoftmax`。facade 到達経路は既存 `Var` 再エクスポート経由）。#1596 で §1.2「LayerNorm／RMSNorm／BatchNorm」行を実装済みへ更新（`docs/norm-ops-design.md`）
    ├── compat-feature-gap.md # fandhe-ai 公開面（facade）と PyTorch／TensorFlow の機能ギャップ表（対象 HEAD `097bff19`。低レイヤー診断 artifact §2 由来。Var 演算 15 個・`nn::Module` 4 種・損失 2 種・optimizer 2 種・scheduler 2 種・f32 固定 dispatch 等の欠落を実装コード読解で確定。イシュー `1574`）。末尾に #1594 の追補（softmax／log_softmax 実装済み化。GPU log_softmax はホストフォールバック）・#1597 の追補（permute／broadcast_to〈expand〉／squeeze／unsqueeze／flatten 実装済み化。squeeze／unsqueeze／flatten は Var::reshape への委譲。cat／stack／split は #1598 へ引き継ぎ）・#1598 の追補（cat／stack／narrow／split／split_with_sizes／chunk 実装済み化。3 バックエンド専用カーネルは既定 Unsupported フォールバックのみで性能最適化は別イシューへ引き継ぎ・narrow は #1599 の対象から解消）を追加。#1596 で追補を追記（§2.7 の RMSNorm／LayerNorm 行はスナップショット不変のまま、`BackendOps::rmsnorm`／`layer_norm`・`Var`／`nn` 接続完了・facade 新規公開面なし・GPU backward 未結線を記録。`docs/norm-ops-design.md`）
    ├── crates-io-naming-decision.md # crates.io 公開クレート名（fandhe-ai prefix）の空き確認・最終名ユーザー承認記録（#878/#879）
    ├── crates-io-publishing-order.md # crates.io 公開 6 クレート間 path 依存の version 併記方針（[dependencies] は付与・[dev-dependencies] は strip）・公開順序（トポロジカル順）・workspace.version 一括バンプ運用（#881）
    ├── cuda-streamk-decision.md        # CUDA GEMM StreamK スケジューリングの機構要約・wave 定量化・採否判断（保留 → #1359 で REJECT 確定。#812。§6 で最終 wave 限定・固定順序 fixup 版〈イシュー #1358〉の opt-in 実装完了・再評価条件 2〈決定性〉充足を追記。条件 1〈実機実測〉は #1359 が GB10 実機実測を完了し不充足と確定〈「#1359 実測結果」節〉。ゲート B-1〈残タイル複合判定統計〉・C・D いずれも判定基準未達のため `select_tiled_f32_kernel`／`CudaGemm::new` への本番結線は行わない。opt-in 実装は維持）
    ├── cuda-tensor-core-design.md      # TASK-11.1a WMMA/mma カーネル設計メモ（#60）
    ├── cuda-tf32-optin-api-decision.md # CUDA GEMM の TF32 Tensor Core 経路を opt-in で選択する公開 API（`fandhe_ai::set_cuda_tf32_gemm_enabled`）の設計判断・既定 OFF／fail-closed 方針・framework-compare `--tf32` の C-1/C-2 分割（#1042）。追補（#1355）で `CudaGemmPrecision` 3 モード化（`Fp32Strict`／`Tf32`／`Tf32x3`）・`set_cuda_gemm_precision` API・互換ラッパー意味論・fail-closed 範囲を追記
    ├── cuda-tf32x3-split-single-decision.md # CUDA GEMM 3×TF32（split-single 法。hi/lo 分割・3 回の `mma.sync` 累積）の設計判断（分割式・レジスタ段分割の根拠・smem 2 面分離案の不採用・CUTLASS `mma_tensor_op_fast_f32` と同型の累積順序・FMA 契約例外の明記。親ツリー #1354・承認元 #1338・イシュー #1355）。§8 の誤差分布／純カーネル時間実測記入欄は #1356 で `docs/perf/cuda-tensor-core-tolerance-tf32x3-gb10.md` へ一本化して引き継ぎ・GB10 実機実測完了（2026-09-07）: `#[ignore]` テスト 2 件 FAIL（P1 不成立）につき §11 の 3 択（推奨／条件付き推奨／非推奨）は未選択のまま baseline 提案値（未承認）を記録しユーザー判断へ回した。#1356 のユーザー判断で非推奨・baseline 不承認を確定（2026-09-08）
    ├── device-memory-pool-design.md # デバイスメモリのサイズクラス・プールアロケータ設計（ハンドル非依存 SizeClassPool<H>〈take／put の RAII 返却・record_allocation／record_loan_end／record_release（Mutex 系）と record_pending_return／record_pending_merge（AtomicU64 の lock-free 系。push／take と同一クリティカルセクションで対にして順序逆転を排除）による PoolStats 更新契約・Metal は synchronize 完了まで put を遅延し同期の成否に関わらず pending_return_bytes で可視化した返却待ちを合流・take_one_for_release とバックエンド別フェーズを持つ release_cached のトランザクション型解放〉・PoolConfig／PoolStats・サイズクラス表・寿命・断片化・スレッド安全・解放戦略・REQ-14 解放 API の facade 到達経路・§8 設計確定事項／実装確定事項の区分。#1018 ツリー・#1019）
    ├── device-resident-update-design.md # 学習ループのパラメータ更新デバイス常駐化の設計（更新経路・所有権・数値一致契約。#933 ツリー・#934）。追補: #1479 で resident `GradStaging` の重み勾配をホストへ読み出す公開 API（`facade::Tape::resident_grads_to_host`／`param_grads_to_host`）を追記・#1555 で Metal 実装完了を反映・#1559 で CUDA 実装完了を反映（3 バックエンドとも実装済み・`fill_resident_weight_grad` の汎用ディスパッチにより自動結線）
    ├── facade-device-handle-design.md # デバイスハンドル再利用の公開 API 設計判断（案 B のみ採用・#929/#946 実装済みの追認・#931）
    ├── facade-optimizer-promotion-decision.md # facade optimizer 公開 API 昇格の設計判断（#932）
    ├── git-history-exposure-decision.md # git 履歴残存内部情報・個人メールアドレスの扱い判断・暫定方針（#477）
    ├── guardrail-change-policy.md    # TASK-6.2 判定器変更時フローの明文化（#149）
    ├── guardrail-self-repair-cli.md  # guardrail／self-repair CLI コマンド仕様（#183）
    ├── inference-chain-single-sync-design.md # GPU 推論チェーン単一同期化（`linear_forward_device` の facade 結線）の設計（`DeviceParamStore` への `predict_device_chain` 新設・Metal `*_tracked` failure_token 契約・CUDA ストリーム順序契約整合・bit 同一契約。設計のみ・実装は #1580〈Metal〉／#1581〈CUDA〉へ引き継ぎ。#1579）
    ├── inference-forward-fixed-cost-design.md # 推論 forward の固定費削減（tape 不要経路・活性化デバイス常駐チェーン）の設計・bit-exactness 契約・実測記録（#1028）
    ├── kernel-fusion.md     # TASK-12.2b カーネル融合の適用範囲・限界（複合WLで融合を性能目標の前提にしない。#168）
    ├── license-matrix.md    # 許容依存 8 区分のライセンス可否表（TASK-1.3）
    ├── matmul-vjp-zero-copy-decision.md # matmul VJP の転置ゼロコピー化（`eval::matmul` の stride 対応）・CPU BLIS／CUDA／Metal gemm 結線を別イシューへ引き継ぐスコープ判断・実測記入欄（#1043 ツリー・#1046）
    ├── norm-ops-design.md # LayerNorm 新設・RMSNorm 既存カーネル接続の設計（最終軸限定契約・`eps` 検査・0 サイズ契約・VJP 式・バックエンド別実装形〈CPU スカラー／CUDA 1 warp=1 行 2 パス／Metal persistent simdgroup + Neumaier/scale-ssq〉・facade 到達経路〈既存 `Var` 再エクスポート経由〉・GPU backward 未結線の対象外整理。M4 Max 実機実測完了・DGX Spark GB10 未実測。イシュー #1596）
    ├── metal-grad-reduction-parity-judgment-decision.md # 勾配の長軸縮約（dw 行方向蓄積・bias 勾配の行方向縮約）の Metal 実装形の決定記録。当初は f32 のみの Neumaier 補償和 + 2 の冪 scale で「`f64` 相当」を狙い、事前判定可能な Tier A/B 2 層契約（PR #1666 codex-review 複数回の P1／P2 指摘を経て改訂）を検討したが、binary64 逐次加算の 64bit 整数ソフトウェアエミュレーションでホスト `f64` 逐次和と bit 完全一致させる方式へ転換し不採用（検討経緯のみ本文に残す）。**契約確定・実装済み（PR #1659〈イシュー #1566〉2026-09-12 マージ。§6 に実装記録〈ファイル・関数名・M4 Max 実機 bit 一致テスト pass〉を追記済み）**
    ├── oss-comparison-harness-decision.md # OSS 直接比較ハーネス（matrixmultiply・gemm crate・MLX・PyTorch）の恒久化・本体 workspace 外配置の設計判断・matrixmultiply/gemm の許容依存第 9 区分〈ベンチ比較対象〉としての条件付きユーザー承認記録（#755）
    ├── perf/                # 性能実測・下限確定の記録群（`performance-floor-decision.md` ほか。GEMM 最適化ツリー #479 の実測記録を含む）
    │   ├── performance-floor-decision.md # REQ-8 段階的下限の確定判断・追補記録（#158・#386・#393・#577）
    │   ├── gemm-optimization-baseline.md # REQ-8 GEMM 5 行の分母・分子（対象カーネル・実機・PyTorch 版・出典）の突合基準（#481）
    │   ├── oss-gemm-comparison-baseline.md # OSS 直接比較の再現手順・計測境界・ベースライン・再計測キャンペーン表（#755）
    │   ├── cuda-parity-baseline.md # CUDA Tensor Core 経路 parity 非後退契約のベースライン記録（#491。#1158 で f16 MatrixUnit 経路 mma 優先化の GB10 非後退確認 §12 を追記。#1162 で specialized_mma_f16 の切り分け・再割り当て・最終 sweep 記録 §13 を追記）
    │   ├── sm121-device-attributes.md # sm_121（DGX Spark GB10）デバイス属性・L1/L2 実効帯域の実測記録（#482）
    │   ├── cuda-gemm-bottleneck-diagnosis.md # CUDA GEMM M=N=K=4096 データ再利用崩壊の定量診断（#486）
    │   ├── metal-gemm-bottleneck-diagnosis.md # Metal GEMM 1024 以降スループット頭打ちの定量診断（#487。#744 是正前・context_cache 導入前の前提。再診断は metal-gemm-bottleneck-rediagnosis.md を参照）
    │   ├── metal-gemm-bottleneck-rediagnosis.md # Metal GEMM 1024 以降頭打ちの context_cache 後の再診断（M4 Max 実機実測完了。fandhe-ai 自系列内の転送〈アップロード＋readback〉寄与は確認したが、candle 側転送分離測定〈#1103 追補〉の結果 candle比ギャップの主因とは確定できず・GPU counters は引き続き未計測〈GPU Service が対象デバイス非対応と報告〉・タイル形状は現行 CANDIDATES[3] が 4096 で最良・1024/2048 は [5]/[6] と同等〈相対 5% 未満〉。#1036・#1103）
    │   ├── metal-gemm-splitk-shapes.md # split-K 対象形状（K 支配的非正方）の劣化定量化実測記録（#810）。#1308 で M4 Max 実機実測（5 プロセス起動 5 回計測中央値）を完了・§4/§6 確定。対象 12 点中 9 点（`actual_groups < 40` 該当）全点で劣化率中央値が事前登録基準 `<0.7` を 5/5 run 一貫して満たし「採用検討推奨」と判定
    │   ├── metal-gemm-splitk-two-pass.md # split-K 2 パス GEMM カーネル（パス 1: K 区間分割・パス 2: 固定順序 Neumaier 補償和縮約）の opt-in 実装記録（#1474 時点は `dispatch_auto` へ未結線。#1516 で定数ゲート付き結線済み・当初既定 OFF → 2026-09-11 ユーザー判断で既定 `true`）。AC-1（run-to-run bit 同一）・AC-4（REQ-8 境界検査）・AC-5（本番経路非後退）は達成。AC-2 は実機実測（M4 Max）で「split-K はパーティション分割そのものに起因し縮約側改善では解消しない厳密ゼロ fail 不成立」が判明し、当初は CUDA 側 `ParityBaseline` と同型の実測ベースライン非後退方式（`tests/common/splitk_parity_baseline.rs`）へ判定方式を変更していたが、PR #1496 の codex-review 指摘（当該 spec 追記は TF32/f16 Tensor Core 経路限定で Metal f32 split-K への適用拡張は未承認）を受け厳密ゼロ fail 判定（`assert_parity`）へ差し戻し・未承認のため 11 形状中 8 形状で実機実行時に既知 FAIL が残る（§5.5）。性能 A/B は #1475、本番結線可否は #1476 へ引き継ぎ。イシュー #1474。#1476 は結線せずと確定（`docs/backend-metal-splitk-decision.md` §4）。#1518 で §6／§8 に forward pointer 追記（結線自体は #1516 で完了・現行状態は decision.md §5 を参照）。#1512 で §5.8 追記: イシュー #1511 の承認〈`docs/backend-metal-splitk-parity-judgment-decision.md` §7〉を受け `gemm_splitk_parity.rs` の判定を `assert_no_split_k_parity_regression`（baseline 非後退方式）へ再切替・Linux 実行可能な契約テスト `tests/splitk_parity_baseline_contract.rs` を新設。実機〈Apple M4 Max〉での全形状 pass 確認は未実施のまま記入欄を残す。#1513 で §5.9 追記: `SPLIT_K_NUMERIC_CONTRACT_APPROVED` を `true` へ切替し自動判定入口 `dispatch_split_k_strided_prepared` のゲートを解除（本番経路 `dispatch_auto`／`MetalBackendOps::gemm` は不変・grep で非到達を確認）・Linux 実行可能な集合検査（`splitk_parity_baseline_contract.rs::approved_baseline_shapes_are_split_k_eligible`）と実機 `#[ignore]` 受け入れテスト（`tests/gemm_splitk_auto_entry_parity.rs`）を新設。M4 Max 実機実測は未実施のまま記入欄を残す。#1547 でコンパイル時定数ゲート `SPLIT_K_DISPATCH_AUTO_PRODUCTION_ENABLED` とドリフト検出テストを撤去し、実行時トグル（#1545）へ一本化（`docs/backend-metal-splitk-decision.md` §5「定数ゲートの撤去（#1547）」参照）
    │   ├── logs/metal-gemm-splitk-two-pass-1474/ # 上記の bit_match／parity／対象 11 形状×4 転置パターン全数実測（parity_survey_all_shapes）・既存 `#[ignore]` 群非後退確認の実行ログ・env_info（内部ホスト名は含めない。イシュー #1474）
    │   ├── metal-gemm-splitk-f64-truth.md # split-K 到達 11 形状（NN・単一乱数系列）で split-K 経路・classic 経路・CPU f32 参照実装の各出力をホスト `f64` 厳密解と突き合わせた誤差実測の診断専用記録（判定基準なし・`assert_no_split_k_parity_regression` の baseline・tolerance 定数・既存テストは不変）。本実測の範囲では split-K 経路が classic 経路・CPU f32 参照実装より `f64` 真値に近かった（全 11 形状で `max_abs`／`mean_abs` とも split-K が下回る）。M4 Max 実機実測 2 run（bit 単位で完全一致・決定的）。イシュー #1549
    │   ├── logs/metal-gemm-splitk-f64-truth-1549/ # 上記の run1／run2 実行ログ・env_info（内部ホスト名は含めない。イシュー #1549）
    │   ├── logs/metal-gemm-splitk-parity-baseline-1512/ # baseline 方式再切替（#1512）の M4 Max 実機確認オーケストレーション（`run_parity.sh`・事前登録判定規則・README）。本 PR 実行環境に Apple Silicon 実機がないため実測ログ自体は未生成（記入欄のみ。イシュー #1512）
    │   ├── logs/metal-gemm-splitk-auto-entry-1513/ # 数値契約ゲート解除（#1513）の M4 Max 実機確認オーケストレーション（`run_auto_entry.sh`・事前登録判定規則・README）。本 PR 実行環境に Apple Silicon 実機がないため実測ログ自体は未生成（記入欄のみ。イシュー #1513）
    │   ├── metal-gemm-splitk-ab.md # split-K 経路と現行 classic 経路の M4 Max 実機 A/B（対象 9 形状・A′〈タイル構成効果〉・対照 3 形状・強制 split-K・フロア）・#1308 の「因果は仮説」留保の実測更新（A′/A がほぼ 1.0・target の改善が K 分割に帰属することを支持）。3 run（事前登録 5 run のうち時間制約で 3 run 完了）で暫定 ADOPT・全 9 対象形状が事前登録基準 `speedup>=1.5` を 3/3 run 一貫して満たす。5 run 完了による正式確定は §7 フォローアップ（未実施）。イシュー #1475。#1476 で §9 に本番結線可否の確定（結線しない）を追記（#1518 で §7／§9 に forward pointer 追記。結線自体は #1516 で定数ゲート付き〈既定 OFF〉に完了）。#1515 で §10（5 run 正式確定の事前登録規則・記入欄。専有ゲートなし・record_only 運用）を追記・実測は Mac セッションへ申し送り（本 PR 時点では未実測）。#1538 で 2026-09-11 に M4 Max 実機 5 run を record_only・共有負荷下で完走し §10.4 に **ADOPT を正式確定**（対象 9 形状すべて中央値 1.55〜3.75 倍・5/5 run 一貫・対照 3 形状 0.98〜1.01・phase0 checksum 5 run bit 同一。`docs/perf/logs/metal-gemm-splitk-ab-5run-1515/` に生ログ・aggregate.md）
    │   ├── logs/metal-gemm-splitk-ab-1475/ # 上記の run{1,2,3}.log・self_check.log・aggregate.py／aggregate.md・env_info.txt・uptime_before_run{1,2,3}.txt・pmset_therm_{before,after}.txt・run_gated.sh（内部ホスト名は含めない。イシュー #1475）
    │   ├── logs/metal-gemm-splitk-ab-5run-1515/ # 5 run 正式確定（イシュー #1515。専有ゲートなし・共有負荷下・record_only 運用）の計測スキャフォールド（`orchestrate.sh`〈`--dry-run` あり〉・`aggregate.py`〈`--self-test` あり。checksum 一致・env_guard 記録・負荷推移の機械検査を追加〉・README・`env_info.txt` 記入欄）。本エージェント実行環境に Apple Silicon 実機がないため実測ログ自体は未生成（記入欄のみ。Mac セッションでの実測手順は README・`docs/perf/metal-gemm-splitk-ab.md` §10.3 を参照。内部ホスト名は含めない）
    │   ├── metal-gemm-splitk-framework-compare-1517.md # split-K 本番結線（#1516・`SPLIT_K_DISPATCH_AUTO_PRODUCTION_ENABLED`）の framework-compare 実践規模 A/B（gemm 8 セル＋train 2 セル。両腕とも `crates/facade` path patch）の帰属表（`crates/backend-metal/tests/splitk_train_shape_attribution.rs` の機械生成出力込み。gemm は形状条件で・train の reuse／backward は入口条件〈backward は形状条件でも〉split-K に非到達だが、train の fresh は L2 forward〈非融合 matmul〉が `dispatch_auto` を経由するため結線後は split-K に到達しうると確認。PR #1531 レビュー指摘で当初の「train は全て非到達」記述を訂正）・事前登録判定規則（非後退 `ratio<=1.00`・checksum 完全一致・後退セルの結線維持可否 3 値判定）・記入欄。本エージェント実行環境に Apple Silicon 実機がないため実測は未実施のまま Mac セッションへ申し送り（イシュー #1517。**#1548 で §6a 追記（事後監視・記録のみ）**）
    │   ├── logs/metal-gemm-splitk-framework-compare-1517/ # #1517 M4 Max 実測完了（2026-09-11）: checksum 全 10 セル一致・後退 4 セル（gemm 3・train 1）で規則どおり**結線維持不可**・ゲート `false` 維持。その後 #1516 のマージでユーザー判断により `true` へ切替（判定記録は不変）。生ログ・monitor・procs・uptime・pmset・JSONL・`compare-gemm-1517-run1.md`・`compare-train-1517-run1.md`・env_info（内部ホスト名はマスク済み。イシュー #1517）
    │   ├── logs/metal-gemm-splitk-framework-compare-1548/ # #1548 M4 Max 実測完了（2026-09-12・実行時トグル・単一バイナリ A/B）: run2（低負荷）で規則 1 不成立 7 セル（規則上は結線維持不可相当の記録）・符号一貫セル 0・checksum 全一致。原因帰属は判定と分けて記録（後退セルはいずれも split-K 非到達で計測ノイズと整合するがホスト側経路判定のオーバーヘッド寄与は未分離）・#1544 ユーザー判断の既定 ON は不変。生ログ・JSONL・manifest・`compare-gemm-1548-run2.md`・`compare-train-1548-run2.md`・env_info・run1（参考・高負荷）の同ファイル群（内部ホスト名は含めない。イシュー #1548）
    │   ├── logs/metal-gemm-splitk-shapes-1308/ # 上記 #1308 の M4 Max 実機実測 5 run 生ログ・`aggregate.py`（python3 標準ライブラリのみ）／`aggregate.md`・`env_info.txt`（負荷ゲート試行履歴含む）・`smoke_run.log`・`pmset_therm_{before,after}.txt`（内部ホスト名は含めない）
    │   ├── cuda-fresh-gemm-n2048-overhead-diagnosis.md # fresh モード CUDA GEMM N=2048 固有の約 166〜184 ms 残存オーバーヘッド診断（DGX Spark GB10 実機実測完了・HEAD 時点で非再現を確認・コード修正なし。#956・#1025。#1157 で §11 追記: #1130 ツリー〈#1146/#1149/#1153〉の結論と #956/#1025 非再現の関係。確定機構〈32 MiB 固定上限〉は #956 の 16 MiB を説明せず別原因・同族の可能性は推測として区別・N=4096 異常値は #1130 と整合・対策なし〈環境要因〉）
    │   ├── burn-wgpu-metal-gemm-zero-result.md # framework-compare の Burn(wgpu) Metal GEMM N>=512 全ゼロの原因切り分け（upstream 既知バグ。#965）
    │   ├── cuda-tensor-core-tolerance-opt-remeasurement.md # opt 版 WMMA TF32 カーネルの数値一致誤差分布再実測（GB10 実機計測完了・sm_86 との差分なし。#994・#995）
    │   ├── cuda-tensor-core-tolerance-gb10-scale-sweep.md # GB10（sm_121）実機での入力スケールスイープ再実測・sm_86 との世代差記録（#995）
    │   ├── cpu-gemm-candle-cpu-retune.md # CPU GEMM マイクロカーネル・packing 再チューニング（pc 外側ループ・A 1 回 pack 候補〈SharedBPcOuter〉。対 gemm crate 逆転狙い。GB10・M4 Max とも実機実測完了・いずれも非採用と結論〈#1140・#1141〉。#1041。#1144 で本番結線不要（`RowPanel` 維持）と確定・次候補は承認待ち）。§8.1 で候補 3（KC=128〜512 再スイープ。`BlockSizes` 経由）を両実機 5 回中央値実測し REJECT 確定（`docs/perf/logs/cpu-gemm-kc-sweep-1315/`。KC=256 現行既定を維持。イシュー #1315）。§8.2 で候補 1（B 側 laneq ベクトル転置版 `RowPanelBLaneqVec`）を両実機 5 回中央値実測し REJECT 確定（N=1024/2048 で対 `RowPanel` 比が両実機とも 1.00 未達〈0.96〜1.00 倍〉・N=4096 のみ改善〈1.01〜1.14 倍〉。本番結線なし。`docs/perf/logs/cpu-gemm-b-laneq-vec-ab-1318/`。イシュー #1318）。§8.3 で候補 2（`vld1q_f32_x3` 経路 prefetch）の帯域律速根拠を実測し「根拠あり」と確定（`docs/cpu-gemm-prefetch-decision.md` 2026-09-08 追補に実測全文。DGX Spark GB10 が無 pin・大コア pin の両条件で事前宣言ゲート単独成立・M4 Max は再計測時の高負荷によりノイズ大で判定不能・結論は DGX 単独成立により不変。codex-review 指摘 2 件（全コア同時計測区間の同期・`aggregate.py` 代表値算出式）を是正し再計測済み。`unsafe asm!` は未実装のまま承認依頼の要点のみ記録・候補 1〜3 すべて確定・総括は #1321 へ引き継ぎ。イシュー #1319）
    │   ├── logs/cpu-gemm-kc-sweep-1315/ # KC 再スイープ（候補 3。#1315）両実機 5 回独立プロセス中央値実測（bit 一致確認・`kc_sweep_ab_1024_2048`／`_4096` 各 5 run）の生ログ・env_info・集計スクリプト（Python3 標準ライブラリのみ。内部ホスト名は含めない）
    │   ├── cpu-gemm-ic-dynamic-variant.md # ic 限定動的配布 variant（`GemmDriverVariant::IcDynamic`。atomic 行パネルカウンタ・pc ごとの B パネル共有 pack）の実装記録・`RowPanel` との bit 完全一致回帰（`unsafe` 非導入。イシュー #1366）。#1367 で両実機（M4 Max・GB10）5 回中央値実測完了・REJECT（不採用）確定（DGX N=1024/2048 が対 RowPanel 比 0.63／0.85 と大きく後退）・本番未結線のまま
    │   ├── logs/cpu-gemm-ic-dynamic-ab-1367/ # 上記（#1367）両実機 5 回中央値実測（bit 一致確認・`ab_1024_2048`／`ab_4096` 各 5 run）の生ログ・env_info・集計スクリプト（Python3 標準ライブラリのみ。内部ホスト名は含めない。イシュー #1367）
    │   ├── cpu-gemm-2d-dynamic-variant.md # (mc, nc) 2D タイル job 動的分配 variant（`GemmDriverVariant::TwoDDynamic`。`partition::job_grid` 純関数・job-local C staging〈S′〉方式・`unsafe` 非導入）の実装記録・`RowPanel` との bit 完全一致回帰（設計 `docs/cpu-gemm-2d-dynamic-partition-design.md`。イシュー #1311）。設計 §5.3 解析 pack 表 6 行を実装出力と全一致確認・大形状〈1024/2048/4096〉bit 一致を M4 Max 実機実測で確認済み。両実機 A/B・採否判定は #1312 で完了（判定 undetermined。§7）。**#1313 で M4 Max 専有ゲート付き Phase 0 再計測を実施し ADOPT 確定・本番結線済み**（§6/§7 追記）
    │   ├── cpu-gemm-2d-dynamic-partition-ab.md # `TwoDDynamic` vs `RowPanel` 両実機 A/B（1024/2048/4096）・採用ゲート判定（イシュー #1312）。DGX Spark GB10（専有ゲート通過）は `jobs_per_worker ∈ {2, 4}` とも全形状で `RowPanel` を 1.09〜1.80 倍上回り #1305 の中止条件も発火せず、Apple M4 Max が専有ゲート不通過（他セッション並走の共有負荷）のため最終判定は **undetermined**（ADOPT でも REJECT でもない）と確定・#1313 へ「結線せず記録のみ」で引き継ぎ。DGX・N=1024 の T10/T8 非単調性（#1305）は「残存」（比 0.86 前後・僅かに閾値 0.90 未達）だが `RowPanel` の 0.48 から大幅改善・全コア軸（T20/T8）ではほぼ解消（比 1.00〜1.03）。**#1313 追記**: M4 Max 専有ゲート付き Phase 0 再計測（attempt=8・load average 3.40 でゲート通過）で jpw=2・jpw=4 とも Tier 1 条件を両実機で充足し **ADOPT 確定**・`TWO_D_DYNAMIC_PRODUCTION_ENABLED = true` で本番結線・framework-compare gemm cpu 両実機・全 12 セル非後退（ratio 0.60〜0.96・改善方向・checksum 完全一致）を確認
    │   ├── logs/cpu-gemm-2d-dynamic-wiring-1313/ # 上記 #1313 の Phase 0 再計測（gate/uptime/aggregate・env_info）・framework-compare 両実機 before/after（JSONL・manifest・compare 結果）の実行ログ（内部ホスト名は含めない。イシュー #1313）
    │   ├── logs/cpu-gemm-2d-dynamic-ab-1312/ # 上記（#1312）両実機 5 回独立プロセス中央値実測（bit 一致確認・`gemm_blis_two_d_dynamic_ab_1024_2048`／`_4096` 各 5 run・DGX は T=8/10/未設定の 3 軸・Tier 2 用 `oss-gemm-compare` 5 run）の生ログ・env_info（事前宣言ゲート転記）・集計スクリプト（Python3 標準ライブラリのみ。内部ホスト名は含めない。イシュー #1312）
    │   ├── logs/cpu-gemm-reuse-phase-1292/ # CPU gemm reuse 境界のフェーズ分解（Layer A `--phases`・Layer B `gemm_reuse_phase_diag_cpu`。両実機 5 回計測中央値）の生ログ・env_info（内部ホスト名は含めない。イシュー #1292）
    │   ├── cpu-gemm-b-laneq-vec-transpose.md # B 側 laneq ベクトル転置版マイクロカーネル（`GemmDriverVariant::RowPanelBLaneqVec`・`neon::compute_b_laneq_vec`／`transpose_4x4`）の実装記録・`compute_b_laneq`（#748）との NaN 込み bit 完全一致・`RowPanel` との bit 完全一致回帰（イシュー #1317）。#1318 で両実機実測・REJECT 確定（§7。N=1024/2048 で対 `RowPanel` 比 1.00 未達）・本番結線なし・候補コードは維持
    │   ├── logs/cpu-gemm-b-laneq-vec-ab-1318/ # 候補 1（B 側 laneq ベクトル転置版）両実機 5 回独立プロセス中央値実測（bit 一致確認・`variant_ab_1024_2048`／`_4096` 各 5 run・Tier 2 用 `oss-gemm-compare` 5 run）の生ログ・env_info・集計スクリプト（Python3 標準ライブラリのみ。内部ホスト名は含めない。イシュー #1318）
    │   ├── logs/cpu-gemm-prefetch-bandwidth-1319/ # 候補 2（`vld1q_f32_x3` 経路 prefetch）帯域律速根拠の両実機 5 回独立プロセス中央値実測（`microkernel_residency_diag`／`achievable_bandwidth_diag`。DGX Spark GB10 は異種コア〈Cortex-X925／A725〉スケジューリング交絡を検出し `taskset` 大コア pin 条件を追加実測）の生ログ・env_info・DGX オーケストレーションスクリプト・集計スクリプト（Python3 標準ライブラリのみ。内部ホスト名は含めない。イシュー #1319）
    │   ├── logs/cpu-mse-backward-threshold-1578/ # `mse_loss_backward` 逐次フォールバック（#1578）の Phase 0（`mse_backward_threshold_sweep` 5 プロセス起動）・Phase 1（framework-compare train A/B・`run_ab_1578.sh`）実行ログ・判定表・env_info（M4 Max・GB10 各ディレクトリ。内部ホスト名は含めない）
    │   ├── logs/elementwise-vjp-backend-ops-1583/ # elementwise VJP BackendOps 経由化ゲート（#1583）のマイクロベンチ A/B 5 run 生ログ（`m4max/`＝cpu・`m4max-metal/`＝metal・`gb10/`＝cuda。各 `before_run{1..5}.log`／`after_run{1..5}.log`／`aggregate.md`／`env_info.txt`）・集計スクリプト（`aggregate.py`。python3 標準ライブラリのみ）。3 バックエンドとも REJECT 確定・勾配 bit-fold は全セル一致（内部ホスト名は含めない）
    │   ├── train-linear-epilogue-fusion.md # 学習 forward の Linear+ReLU epilogue 融合（gemm_bias_act／gemm_resident_rhs_act 結線）の起動数 before/after・CPU 実測・Metal/DGX Spark 未実測の明記（#1044）
    │   ├── train-step-phase-breakdown.md # CPU / CUDA / Metal 学習 1 step のフェーズ分解実機実測（M4 Max・DGX Spark GB10・5 回計測）・支配項トップ 3（backward が 83.6〜97.3% で全バックエンド共通の支配項）・#1008 配下 Issue 優先順位の更新案（#1010）。#1145 で v0.6.0 ピン再計測（backward は 75.1〜97.5% で支配項の結論は不変）・0.4.0 との差分を §10〜§14 に追記（推論のフェーズ分解はハーネス制約により未実施）。#1151 で v0.6.0 実測の支配項トップ 3 確定・backward 内部の推定・infer 未達の仮説・改善 issue 起票案（#1211〜#1219）を §15 に追記。#1350 で CUDA Graph step capture（#1349・`--graph`）有無の GB10 3 状態（off/stream-only/on）5 回中央値 A/B・launch 回数診断カウンタを §16 に追記: `step_total` ON/OFF 比 0.9552〜0.9644（事前宣言基準 ≤0.95 未達）・`device_update` 比は中央値 0.9719 だが 5 run 中 1 run 符号反転（5/5 一貫の基準未達）・checksum 全セル完全一致・事前宣言基準により既定 OFF 維持（中立）と確定
    │   ├── lowlayer-diagnosis-2026-09-12.md # 低レイヤー診断 artifact §7 の実測転記（GB10 0.8.0 ピン・専有／M4 Max origin/main HEAD `097bff19` path patch＋backward 内訳計装・共有負荷）。CUDA train/infer 実測・has_async_alloc／TMA プローブ・#1214 NT/TN parity（§2）・小形状スレッド方針が機体で逆向き〈M4=上限／GB10=大コア親和性〉（§3）・backward 非 GEMM 残差 18〜34%（MSE backward の rayon fork-join 固定費・reuse マスクの非連続 stride が主因。§4）・Metal readout legacy 後退の matmul 区間局所化（#1520 続き。§5）・issue ツリー草案改訂（§7。イシュー `1574`）
    │   ├── logs/lowlayer-diagnosis-2026-09-12/ # 上記の DGX／Mac 生ログ・JSONL・オーケストレーションスクリプト・診断専用計装パッチ（`diag-instrumentation.patch`。本番実装ではない）・Python 参照フレームワークハーネス（`bench_py.py`）・`results.jsonl` 誤追記の差し戻しバックアップ。内部ホスト名は含めない（イシュー `1574`）
    │   ├── train-reuse-relu-mask-stride.md # reuse backward の ReLU マスク（`elementwise_mul_mask`）を非連続 transpose view（`Op::LinearResident` の `d_input`）に stride 対応させる実装記録（`MaskReadOperand` の `Contig`／`View`〈`usize` プレーン演算〉／`Owned` 3 分岐・全 contig 高速経路・単体テスト 7 件・CPU／Metal bit ダンプ before/after 完全一致）。backward の実際の GEMM 呼び出し回数は fresh／reuse とも 4 回で同一であることをコード読解で確定（回数削減ではなく事実整理）。M4 Max framework-compare train A/B は round1（初版実装。連続経路が旧実装比 約 5.6 倍退行しておりガード・対象セルとも後退。REJECT）→ round2（是正後の最終実装。cpu reuse 0.9196・metal reuse 0.9653 で両対象セルとも非後退。ADOPT）の 2 段階を記録。GB10（CUDA）は本実装エージェントの実行環境に到達手段がなく未実測（イシュー #1577）
    │   ├── logs/train-reuse-relu-mask-stride-1577/ # 上記の round1／round2 framework-compare 生ログ（JSONL・`compare_gemm_ab.py` 判定表・uptime）・CPU／Metal bit ダンプ before/after・`run_ab_1577.sh`・env_info（内部ホスト名は含めない。イシュー #1577）
    │   ├── cuda-async-sync-removal-framework-compare-ab.md # CUDA 都度同期廃止（#1011）の framework-compare 実践規模 A/B 計測記録（DGX Spark GB10 実機計測完了。受入根拠は同一プロトコルの fresh 0.928 倍のみ。reuse 0.440 倍〈約 2.3 倍短縮〉は #1059 の resident forward/backward 経路変更との複合効果につき参考値〈codex-review P1 対応〉・checksum 複合判定 ok。#1083）
    │   ├── cuda-tf32-optin-parity.md # CUDA TF32 opt-in GEMM（`crate::precision`）の複合判定・実機実行手順・実測記入欄（本エージェント実行環境に CUDA 実機なしのため未実測明記。#1042）
    │   ├── cuda-wmma-f16-perf-triage.md # WMMA(f16) 性能外れ値（`wmma_f16_opt`≈`wmma_f16_basic` が `mma_sync_f16` を約3〜13倍〈形状依存。dim2048で約7倍・dim4096で約11〜13倍〉恒常的に下回る・tiled f32 カーネル単体も下回る〈GB10実測。#64 f16 assert red〉）の診断・GB10実機実測（2026-09-03）・大形状計測をカーネル単体プロトコルへ切替えたテスト是正記録・到達性整理・切り出し先（#1130・#1131）。§8 で `tensor_core_tflops_record` の f16 計測を本番 f16 経路（`mma_sync_f16`）へ追従させ assert pass 転換・`wmma_f16_opt` はフォールバック限定維持と確定（GB10実機実測2026-09-04・#1160）。`MMA_PRIORITY_PRODUCTION_ENABLED` 本番結線は PR #1179 codex-review 指摘〈K=4096 非後退ゲートの `MmaF16` baseline ceiling 未承認〉により `false` へ一時差し戻し後、#1190 の ceiling 承認・反映を経て #1191 で `true` へ復帰・本番有効化済み（`docs/perf/cuda-gemm-auto-f16-mma-switch.md` §0・GB10実機実測2026-09-05）。#1123
    │   ├── cuda-large-buffer-percall-alloc-transfer-threshold.md # 大容量バッファ per-call アロケーション＋転送（`clone_htod`／`alloc_zeros`／`clone_dtoh`）のP0〜P6フェーズ分解・サイズスイープ（8〜64 MiB・12段階）実機実測（GB10・2026-09-03。PR #1169 codex-review・Bugbot 指摘対応のsynchronize是正〈P0含む全フェーズ〉後にGB10実機で再実測完了）。閾値32 MiB自体（glibc mmapしきい値・H-B/H-C棄却）は事実として確定・再現済みだが、これが#1123の元のper-call D2H症状（dim4096合算約261〜263ms）の主因であることは未確定（dim4096相当規模での直接再現・降順走査限定の確率的スパイクの発生条件特定が必要）・32 MiB通過直後の降順走査でのみ確率的に発生する追加スパイク（二峰性）も記録。32→33 MiBの別段差（デバイス確保・H2D側）も新規に観測・未解明（#1146）
    │   ├── cuda-percall-alloc-pool-threshold-ab.md # cuMemPool release threshold 引き上げ・cuMemAlloc 同期割当への切替の A/B 計測（なし／A／B／A+B の4条件・P0〜P4＋CudaMmaGemm本番経路レプリカP7〈dim4096〉）。#1146 §4.3の32→33 MiB段差・#1130元症状の対策候補を決着させる位置づけ。GB10実機実測完了（イシュー#1153でPhase 0として実行）: 孤立マイクロベンチマーク（P1〜P3）ではrelease threshold引き上げ（案A）が段差比1.00〜1.03倍へ縮小したが、`CudaMmaGemm::run_f16`の現実的レプリカ（P7）では同じ案Aがmedianを約10倍悪化させる正反対の結果となり、案Aの本番結線は不採用と確定（GB10 unified memory環境での物理メモリ競合と推定）。#1149
    │   ├── cuda-gemm-tiled-naive-speedup-4096-triage.md # `gemm_tiled.rs::tiled_f32_outperforms_naive_at_4096` の新規 FAIL（speedup=0.235x。#1162 GB10 実機 sweep・直列条件）の再現・原因切り分け。単発実行 5 回中 1 回（0.189x）・直列バイナリ実行 3 回中 1 回（0.208x）で FAIL を再現し、cp.async パイプライン結線（#1137/#1164/#1344）を経由しない classic 版カーネルでも本番 pipeline 版と同振幅の slow モードが発生することを確認し、原因が tiled カーネル自体ではなく naive/tiled 共有ホスト経路側にある可能性を示す強い傍証を得た（計測点は H2D／確保／カーネル／readback を含む呼び出し全体の壁時計時間でありフェーズ分解計測ではないため、#1130 系〈64 MiB D2H 確率的二峰性〉との一致は仮説段階。§6）。`docs/backend-cuda-real-device-testing.md` §5.1 の「並列実行のみ fail」という従来の帰属を本テストについては再評価。GB10実機実測2026-09-07。イシュー #1203
    │   ├── logs/cuda-gemm-tiled-naive-4096-1203/ # 上記の単発 5 回・直列バイナリ 3 回・診断テスト（classic vs pipeline・順序反転・インターリーブ）1 回の実行ログ・env_info（内部ホスト名は含めない。イシュー #1203）
    │   ├── cuda-gemm-mma-f16-pool-wiring.md # `CudaMmaGemm::run_f16`のper-call確保（`clone_htod`／`alloc_zeros::<f16>`直呼び）のSizeClassPool経由化を試作（#1153）。`pool.rs::CudaSliceHandle`のdtype一般化（`PoolDtype`トレイト・第2プール非新設）・`gemm_mma.rs`のview ベース起動実装集約とpooled API（`pub`・`internal-diagnostics`feature限定）追加。GB10実機の別バイナリ比較・同一バイナリ交互実行の両方でdim4096がプール経由化により明確に悪化（slow-path到達率raw 50%対pooled 17%）することを確認し、`run_f16`への本番結線は見送り（512〜2048は改善）。数値一致回帰は既知failのfail_countまで含めbefore/after完全一致
    │   ├── cuda-gemm-tiled-f32-swizzle-ab.md # tiled f32（classic）経路へのブロック実行順スウィズル（#1034）横展開の判定基準・#1164 後の到達性整理（整列N=1024/2048/4096はpipeline経路のためclassic非到達）・GB10実機実測（ゲート0 PASS・ゲート1がN=512で判定基準未達のため不採用〈REJECT〉。`CudaGemm::new`への結線は行わない。#1139）
    │   ├── cuda-gemm-tiled-pipeline.md # cp.async 多段パイプライン（#1033）の GB10 実測記録・本番結線判断（イシュー #1137。bit 一致・parity 0 fail・N=1024/2048/4096 で 1.51〜1.74 倍改善を確認し `CudaGemm::run_tiled_f32` 系 3 入口へ形状条件付きで結線〈ADOPT〉。GB10実機実測2026-09-03）。#1343 で 128×64×16 f32 pipeline カーネル（8×4 レジスタブロック・A フラグメント XOR スウィズル・cp.async zfill 境界。`kernels_tiled_pipeline_128x64.rs`）を opt-in で追加し、ホストモデル bit 一致・実機 `#[ignore]` bit 一致自己検証テストを整備（実機未実行・#1344 へ性能実測と結線可否判断を引き継ぎ。本番既定経路は不変）。#1344 で GB10 実機実測（純カーネル時間 5 回中央値: N=1024 1.05 倍・N=2048 1.12 倍・N=4096 1.35 倍。N=512 以下は優位性なし）に基づき第 2 スロット方式（`tiled_pipeline_128x64`。64×64 常時コンパイル＋128×64 を条件付き追加）へ拡張し、N≥1024 かつ K≥1024 の形状で 128×64 を選択する結線を採用（`TILED_PIPELINE_128X64_PRODUCTION_ENABLED = true`・`TILED_PIPELINE_128X64_MIN_N`/`_MIN_K = Some(1024)`）。bit 一致・parity 0 fail（既知無関係 fail 2 件除く）・本番ディスパッチ非後退（N=2048/4096 で 6.5〜7.6% 改善）を GB10 実機実測で確認済み〈ADOPT〉。#1347 で persistent タイルキュー版（64×64・128×64 両タイル）の GB10 実機実測（5 回計測中央値・`--blocks-per-sm auto`）を実施し、N=1024 で 64×64=1.0063 倍・128×64=1.0182 倍と事前判定基準〈≥1.05〉に届かず**不採用〈REJECT〉と確定**（`select_tiled_f32_kernel`／`CudaGemm::new` への結線なし。GB10実機実測2026-09-07）
    │   ├── logs/cuda-tiled-pipeline-128x64-1344/ # 上記 #1344 のゲート A〜D 実行ログ・env_info（内部ホスト名は含めない。イシュー #1344）
    │   ├── cuda-gemm-tiled-pipeline-persistent.md # persistent タイルキュー版 f32 pipeline カーネル（grid=SM 数・atomic タイル取得。イシュー #1346）を opt-in で追加（`kernels_tiled_pipeline.rs` の断片分割・`gemm.rs::PersistentTiledPipelineFunction`）し、タイル内計算本体（`TP_TILE_CORE`）を非 persistent 版と完全共有することで出力 bit 同一を機構として保証。静的テスト（`atomicAdd` 1 箇所限定・REQ-8 境界検査維持）・実機 `#[ignore]` bit 同一自己検証テスト・GPU-only A/B 計測ハーネスを整備（実機未実行・#1347 へ性能実測と結線可否判断を引き継ぎ。本番既定経路は不変）。#1347 で 128×64 persistent 版（`kernels_tiled_pipeline_128x64.rs` の断片分割・`compile_tiled_pipeline_persistent_128x64_variant`）を追加実装し 64×64/128×64 両タイルの bit 同一・GB10 実機性能実測を完了・**REJECT と確定**（§5.2／§6 に転記。実測詳細は `cuda-gemm-tiled-pipeline.md`「#1347」節を正とする）
    │   ├── cuda-gemm-tiled-pipeline-streamk.md # 最終 wave 限定 Stream-K（固定順序 fixup。イシュー #1358・親 #1357・承認元 #1338）を opt-in で追加（`kernels_tiled_pipeline.rs` の `TP_SK_KERNEL_PREFIX`／`TP_SK_TILE_CORE`／`TP_SK_FIXUP_KERNEL`・`gemm.rs::streamk_plan`／`StreamKTiledPipelineFunction`）。部分和は一意スロットへ書き・fixup は寄与者昇順の固定順序で直列加算する設計で `docs/cuda-streamk-decision.md` の fixup 非決定性懸念を解消。配布計画の網羅性・一意性を GPU 不要のホストシミュレータテスト（広範な `(T,G,nk)` 全探索）で機械検証・`--streamk` ベンチ列追加。#1359 で GB10 実機実測を完了し §6 を実測値で確定（ゲート A 8/8 PASS・ゲート B-1 残タイル複合判定統計 16 行中 12 行 `fail_count>0`・ゲート B-2 既存経路非後退 28/28 PASS・ゲート C `streamk_over_pipeline3` 5 回中央値 N=1024 1.0271 倍〈<1.05 未達〉・N=2048 0.9395 倍〈<1.00 未達〉・ゲート D〈64×64 streamk / 128×64 pipeline3〉N=1024 0.9801 倍・N=2048 0.8394 倍〈いずれも <1.00 未達〉）。**REJECT と確定**（`select_tiled_f32_kernel`／`CudaGemm::new` への結線なし。opt-in 実装は維持。本番既定経路は不変。GB10実機実測2026-09-07）
    │   ├── logs/cuda-tiled-pipeline-persistent-1347/ # persistent タイルキュー版（64×64・128×64）の GB10 実機ゲート A/B/C 実行ログ・env_info（内部ホスト名は含めない。イシュー #1347）
    │   ├── logs/cuda-tiled-pipeline-streamk-1359/ # Stream-K 版 GB10 実機実測（イシュー #1359）のゲート A〜D 実行ログ・env_info・aggregate.py／aggregate.md（内部ホスト名は含めない。REJECT 確定）
    │   ├── cuda-managed-placement-ab.md # CUDA managed memory 配置（#1352）有無の framework-compare gemm cuda fresh／reuse・train fresh／reuse A/B 計測（GB10 実機実測完了。train reuse が 1.71 倍後退〈`device_update` フェーズ単独では 2.82 倍〉・gemm／train fresh は managed 非到達設計どおり差なし・CPU 側ページ経由アクセス帯域〈readback〉は是正前実装の実測で差 5% 未満（是正後実装〈managed ページ直接読み取り〉での再計測は未実施のため低下有無は未確定。#1397 レビュー指摘）・checksum 全セル完全一致。既定化可否判定は **REJECT（既定 OFF 維持）** と確定。フルマトリクス〈gemm N=1024/4096・infer 副次計測〉は時間制約により未実施のままスコープ外として明記。イシュー #1353）
    │   ├── logs/cuda-managed-placement-ab-1353/ # 上記の実行ログ・JSONL・帯域テストログ・契約テスト非後退確認ログ・env_info（内部ホスト名は含めない。イシュー #1353）
    │   ├── cuda-gemm-candle-gate-remeasurement.md # FP32 SIMT GEMM N=1024/2048/4096 reuse の candle 比 5 回計測中央値再計測・#1031 ゲート達成判定の確定記録（正式系列〈fandhe-ai =0.6.0〉・参考系列〈#1164 結線後 HEAD〉の 2 系列併記・N=2048 candle 無効データの原因・再現条件記録。GB10 実機実測。イシュー #1142。#1184 で fail 2 要素の実値・厳密真値突合を §5.3 追記。#1185 で正式系列 `fandhe-ai =0.7.0` を 2026-09-06 に再計測し §11 追記: 0.381／判定不能／0.904 倍で参考系列と誤差範囲内一致・#1031 未達成を確定。#1360 で Phase 4／5〈#1342 の 128×64 cp.async pipeline 本番結線・#1337 借用ビュー readout〉反映後の再計測を §12 追記: 正式系列はピン未更新のため §11 と同値〈未達成再現〉・参考系列 readout-on は N=4096 のみ達成〈1.433 倍〉するが N=1024/2048 は大幅後退し形状依存の混在結果と判明。#1337 で §13 追記: #1360 §12 を正式記録として整理し DGX 独自実行〈わずかに後の sha〉で同一符号を再現・公正性の論点〈candle ハーネス不変・#1336 非到達〉を明記。#1260 で §14 追記: tolerance 契約変更〈#1241 承認・#1247/#1250 実装〉下で正式系列のみ再計測し N=2048 の「判定不能」が解消・確定判定〈未達・0.476 倍〉へ遷移したことを実測確認・N=1024/4096 は誤差範囲内で再現）。#1438 で §15 追記: 借用ビュー readout の既定経路化（旧 host-view-readout cargo feature 撤去）を同一 facade ソースで揃えた before/after として GB10 実機再計測し、全 3 形状非後退・checksum 完全一致を確認（ADOPT。正式判定は §14 のまま不変）。#1489 で §16 追記: 正式系列 `fandhe-ai =0.8.0`（#1487 ピン更新後）を 2026-09-10 に GB10 で再計測（prebuild で計測からビルドを分離・事前宣言の専有ゲート通過後）し、0.429／0.496／**1.478** 倍で **N=4096 が正式系列として初めて candle 比ゲートを達成**・N=1024/2048 は未達のまま（正式判定を「N=4096 達成・N=1024/2048 未達」へ更新。§15.3 の参考系列 after 腕と 3 形状とも誤差範囲内一致）
    │   ├── logs/cuda-gemm-candle-gate-1142/ # 上記の実行ログ・env_info（内部ホスト名は含めない。イシュー #1142）
    │   ├── logs/cuda-gemm-candle-gate-1360/ # 上記 §12（Phase 4／5 反映後）の正式系列・参考系列 off/on 3 本の実行ログ・env_info（内部ホスト名は含めない。イシュー #1360）
    │   ├── logs/cuda-gemm-candle-gate-0.8.0-1489/ # 上記 §16（正式系列 `fandhe-ai =0.8.0` 再計測）の実行ログ・判定表出力・専有ゲートログ・prebuild ログ・`orchestrate.sh`・env_info（内部ホスト名は含めない。イシュー #1489）
    │   ├── logs/cuda-gemm-candle-gate-1260/ # 上記 §14（承認済み契約下の N=2048 再計測）の実行ログ・判定表出力・env_info（内部ホスト名は含めない。イシュー #1260）
    │   ├── logs/cuda-gemm-candle-parity-1184/ # N=2048 candle 側 parity fail 2 要素の実値ダンプ・厳密真値突合結果・env_info（内部ホスト名は含めない。イシュー #1184）
    │   ├── candle-parity-tolerance-candidates.md # #1184 ダンプ実値からスケール付き絶対誤差／ULP 判定候補（候補 A/B）を現行複合判定へ OR 追加した場合の fail 数を机上計算（`scripts/bench/framework-compare/parity_tolerance_candidates.py`。K=N=2048・正方・入力 U[-0.5,0.5) の 1 条件限定・「候補で置き換える」判定は本ダンプから評価不能と明記）。推奨・採否は含まない事実（fail 数・緩和上限）のみの記録・#1238／#1239 への引き継ぎを含む（イシュー #1237）
    │   ├── logs/candle-parity-tolerance-candidates-1237/ # 上記の生出力（candidates-2048.md）・env_info（内部ホスト名は含めない。イシュー #1237）
    │   ├── candle-parity-tolerance-baseline-impact.md # 上記候補判定（候補 A/B）を fandhe-ai 本体側の parity 非後退契約（`crates/backend-cuda/tests/common/parity_baseline.rs::BASELINES`。45 行）へ適用した場合の影響を机上確認（`scripts/bench/framework-compare/parity_baseline_impact.py`。行単位の集計値のみのため no-op／全救済／部分・未確定の 3 クラス分類と fail_count 値域まで・「置き換え」判定はスコープ外）。同時更新が必要な箇所一覧（定数ピン 3 系統・判定式レプリカ・リテラル閾値レプリカ）・BASELINES/tolerance 定数は非変更・#1239／#1254／#1256 への引き継ぎを含む（イシュー #1238）
    │   ├── logs/candle-parity-tolerance-baseline-impact-1238/ # 上記の生出力（baseline-impact.md）・env_info（内部ホスト名は含めない。イシュー #1238）
    │   ├── cuda-gemm-reuse-phase-breakdown.md # GEMM reuse 計測境界を H2D／カーネル／D2H／同期でフェーズ分解し、#1142 §4.3 の「H2D/D2H 固定費が希釈要因」推定を精緻化（実際の主因は host_copy／checksum というハーネス診断コスト）。matmul 単体は candle fresh を上回る（N=1024: 1.59倍・N=4096: 1.47倍）ことを確定。N=4096 D2H の二峰性（#1169 関連）は未確定のまま記録。GB10実機実測。イシュー #1182
    │   ├── logs/cuda-gemm-reuse-phase-1182/ # 上記の実行ログ・env_info（内部ホスト名は含めない。イシュー #1182）
    │   ├── cuda-gemm-auto-f16-mma-switch.md # CudaGemmAuto::run_f16 の MatrixUnit 分岐 mma 優先・wmma フォールバック切替（#1156）の前後比較記録。GB10実機実測完了・512/1024/2048 は非後退（1.75〜4.67倍）・4096 は#1130 病態下で base 5run範囲内。本番結線（`MMA_PRIORITY_PRODUCTION_ENABLED = true`）は PR #1179 codex-review 指摘〈K=4096 非後退ゲートの `MmaF16` baseline ceiling 未承認〉により一時差し戻し後、#1190 の ceiling 承認・反映を経て #1191 で `true` へ本番有効化済み（同一 HEAD base/after 再計測で非後退確認。GB10実機実測2026-09-05。§0・「#1191 再計測」節）。#1160
    │   ├── metal-bench-noise-protocol.md # Metal ベンチ A/B（`bench_harness::ab`）のノイズ対策プロトコル（interleaved 比較・順序反転・時間ベースウォームアップ・安定性ゲート `STABILITY_SPREAD_GATE`・実行前環境ガード〈#1264〉・バックオフ再試行〈#1265〉）の設計記録。§8 でロバスト統計（トリム済みレンジ／IQR 外れ値除去後 spread）導入を設計・提案（#1249 実測データ〈計 10 セッション×5 サイズ = 50 セル〉への再適用。どの候補でもセッション単位成立は 10 中 0。イシュー #1266）。#1485 で確定（併記のみ・置換なし・閾値不変・案 E 不採用。§8.6 に承認記録・§8.8 に M4 Max 実機記録）
    │   ├── metal-gemm-transpose-tiled.md # gemm_simdgroup_tiled の転置ロード（TRANS_A/TRANS_B）拡張・NT/TN/TT へのタイル variant 選択適用の実装・実機正確性実測記録（M4 Max。NN 非後退ビット同一・NT/TN/TT parity 確認済み。イシュー #1138）。性能 A/B は #1186 で `gemm_transpose_route_ab_bench.rs` を追加し計測を試みたが、同一マシンで並走する他セッションの GPU 負荷によりフェーズ 1 安定性ゲートが 4 回の調整試行でも不成立のため `verdict=undetermined`（判定不可）。イシュー #1187 で 4 試行（うち 2 回は ROUNDS/COOLDOWN/MIN_WARMUP を増やす方向へ一時調整・試行 4 は低負荷・並走プロセスなしで実行）を追加実行したが、いずれもフェーズ 1 が不成立のまま `verdict=undetermined` が継続（§5.4。試行 4 は低負荷開始でも gate 超過したため他セッション負荷のみでは説明できない可能性を示唆）。`dispatch_strided_bias_act_prepared` への自動ルーティング結線はコード変更なしで未実施のまま。§5.5 でフェーズ 1 単発スパイクの原因（GPU 実行時間／host 側時間）切り分け用に `--gpu-timestamps` opt-in（`MetalContext::synchronize_with_gpu_timestamps` 公開化・`MetalGemm::encode_tiled_prepared` 新設・`bench_harness::ab::run_stability_observed` 新設）を追加し M4 Max 実機で短時間動作確認を完了（既定出力・判定ロジックは不変。正式な原因切り分け実測は #1261 へ引き継ぎ。イシュー #1259）。イシュー #1253 で §5.6 追記: `--phase1-only` を排他環境（load average < 2・他 GPU プロセスなし）で 3 回実行し spread 分布を記録する試みを 2 回（attempt 1・2）実施したが、いずれも worktree 環境自体が複数イシューの並列実行セッションを常時抱えているため load average が収束せず TIMEOUT・phase1-only 実測 0 件のまま（attempt 1 は load1 が一時的に 2.0 未満へ低下した行が 2 行あるが `gate_ok=0`・判定条件は実スクリプト消失により未確定。再構成スクリプトは attempt 1 の判定を再現する保証なし）。#1261 で §5.7 追記: 排他環境（load average < 2.0・他 GPU プロセスなし）での正式な分離計測を試みたが、30 回試行（約 30 分）すべてでゲート不通過（load average 最小 2.40〈#1264 提案閾値の 2.0 を一度も下回らず〉・平均 4.28）となり分離計測データ自体が得られず、原因候補 (a)(b)(c) は「排他環境が確保できず評価対象なし」と記録（Phase 2〈#1263〜#1266〉反映事項を整理・#1266 のロバスト統計優先度を上げる推奨を追記。実行前ガード用フラグ `--min-warmup-secs=<N>` を追加）。イシュー #1255 で §5.8 追記: 負荷環境で `--phase1-only` を 5 run 実行（`high` 分類 1/3・`MAX_RUNS` 使い切りで打ち切り。fail-closed）し、25 セル中 24 で gate 超過を確認。低負荷帯（median_load1 1.8〜2.0）でも spread 超過は解消せず「負荷さえ下がればスパイクが消える」仮説を弱く反証したが、排他環境（#1253）が `valid_runs=0` のままのため「単発スパイクが排他環境でも残るか」自体は確定不能のまま #1257（#1259/#1261）へ引き継ぐ。#1267 で §5.9 追記: #1264/#1265 の内側ガード（`env_guard`）を用い phase 1 → phase 2 の完走を 2 attempt 試みた。内側ガードは 2 attempt とも有限時間（1 回目・28 分のバックオフ後）で通過し #1253/#1261 が一度も突破できなかった入口判定自体は改善したが、phase 1 実測 5〜9 分間の全区間で低負荷が持続する保証は別問題であり、2 attempt とも実測区間内で load average が最大 9.27（attempt 1）／12.37（attempt 2。バックオフ待機中〈実測開始前〉の 15.59 は実測区間外のため除く）まで再上昇し 10 サイズ計測中 9 サイズで安定性ゲート超過・`verdict=undetermined`（フェーズ 2 未到達）に終わった。#1253 由来の外側ゲート（`proc_count == 0` 条件）は本 worktree 環境の常駐 python3 スタブにより構造的に通過し得ないという新知見も記録（`1267-orchestrate.sh`／`1267-aggregate.py` を成果物として追加。実測では内側ガードのみでバイナリを直接起動）。#1270 で §5.10 追記: #1267 の `verdict=undetermined`（phase 2 未到達・セル別 B/A 比なし）により判定基準未達と確定し、`dispatch_strided_bias_act_prepared` への自動委譲は**結線せず**（コード変更なし・NN ビット同一は結線前実装のまま維持）。再開条件（`route_ok` 確定）・結線案メモを §5.10 に記録。#1272 で §5.11 追記: #1270 が結線せずと確定しコード変更ゼロのため framework-compare `gemm metal` の結線前後 A/B は「結線前＝結線後」で計測対象なしと判定し数値は記録せず、代わりに `gemm` タスク（NN・contiguous）が結線候補経路 `dispatch_strided_bias_act_prepared` へ構造的に到達しないこと（到達するのは NT/TN の VJP 経路のみ）・`run_ab_gemm_metal.sh` の before 腕が crates.io ピン未更新（#1438）により構造的にビルド不能なことを実測確認し、再開時のプロトコル（`train --phases` 主ゲート・`gemm` は副次の健全性確認・比率の向きの注意）を事前宣言
    │   ├── logs/metal-gemm-transpose-route-ab-1186/ # 上記 A/B 計測試行の実行ログ・env_info（内部ホスト名は含めない。イシュー #1186）
    │   ├── logs/metal-gemm-transpose-route-ab-1187/ # 同上の再実行（4 試行とも undetermined。試行 4 は低負荷環境・実行中 uptime 記録付き）の実行ログ・env_info（内部ホスト名は含めない。イシュー #1187）
    │   ├── logs/metal-bench-env-guard-1265/ # 環境ガードのバックオフ再試行・env_info 自動記録・`gemm_transpose_route_ab_bench` 結線（イシュー #1265）の `--guard-only` 短時間動作確認（record_only／gated pass／exhausted の 3 モード）ログ・env_info（内部ホスト名は含めない。イシュー #1265）
    │   ├── logs/metal-gemm-transpose-route-ab-1242/ # イシュー #1253: `--phase1-only`（#1249/#1251）を排他環境（load average < 2・他 GPU プロセスなし。ゲート閾値は変更せず）で 3 回実行し spread 分布を記録する試み。attempt 1（2026-09-08・最大 3 時間待機）・attempt 2（2026-09-09・有界待機）とも同一 worktree 環境が複数イシューの並列実行セッションを常時抱えているため load average が 2 未満へ収束せず TIMEOUT・phase1-only 実測は 0 件のまま。attempt 1 の `wait_gate.log` 集計では load1 が一時的に 2.0 未満〈1.86／1.92〉へ低下した行が 2 行あるが `gate_ok=0` のままで、attempt 1 の判定条件は実スクリプト消失により未確定・`wait_gate.sh`／`orchestrate.sh` は attempt 2 用の再構成版で attempt 1 の判定を再現する保証はない（`wait_gate.log`／`wait_gate_attempt2.log`・`orchestrate.log`・`orchestrate.sh`／`wait_gate.sh`・`aggregate.py`・`env_info.txt`。内部ホスト名は含めない。詳細は `docs/perf/metal-gemm-transpose-tiled.md` §5.6）。同ディレクトリに #1261 の排他環境ゲート実測（30 回試行分の `1261-gate.log`・`1261-env_info.txt`・`1261-orchestrate.sh`・`1261-aggregate.py`。`1261-` プレフィックスによりファイル名衝突なし。ゲート不通過につき E1〜E3／W1〜W2 の分離計測ログ自体は生成されていない。詳細は同ドキュメント §5.7）も同居する。同ディレクトリに #1255 の負荷環境実測（5 run 分の `1255-orchestrate.sh`・`1255-aggregate.py`・`1255-phase1_run{1..5}.log`・`1255-phase1_run{1..5}_monitor.log`・`1255-env_info_run{1..5}.txt`・`1255-DONE`・`1255-aggregate.md`。`1255-` プレフィックスによりファイル名衝突なし。詳細は同ドキュメント §5.8）#1267 の改善後プロトコル実測（`1267-orchestrate.sh`〈計画どおりの外側ゲート→内側ガード→BREACH 監視の 3 段構成。構文検証済みだが常駐 python3 スタブにより外側ゲートが構造的に通過し得ないため本イシューの実測では未使用〉・`1267-aggregate.py`〈本計測ログの集計スクリプト。`--self-test` 済み〉・`1267-attempt{1,2}-run.log`／`1267-attempt{1,2}-monitor.log`／`1267-attempt{1,2}-env_info.txt`／`1267-DONE_ATTEMPT{1,2}`／`1267-uptime_*.txt`／`1267-pmset_therm_*.txt`。2026-09-09 実測。内側ガード〈#1264/#1265〉は 2 attempt とも有限時間で通過したが phase 1 実測区間内の負荷再上昇〈load average 最大 9.27（attempt 1）／12.37（attempt 2。バックオフ待機中の 15.59 は実測区間外）〉で 10 サイズ中 9 サイズが安定性ゲート超過・`verdict=undetermined`。詳細は同ドキュメント §5.9）も同居する。同ディレクトリに #1270 の結線判断における参考実測（結線前状態〈HEAD `760db74`〉での `gemm_strided_parity` 全 `#[ignore]` テスト再実行。`1270-parity-rerun.log`〈15 件全 pass〉・`1270-env_info.txt`。詳細は同ドキュメント §5.10）も同居する。同ディレクトリに #1272 の framework-compare gemm metal 結線前後 A/B（計測対象なし）の事実確認記録（`1272-env_info.txt`・`1272-crates-diff.txt`〈`git diff --stat` 読み取り専用出力〉・`1272-pin-guard-before-arm.log`〈before 腕の `bench_fandhe_pin_guard.sh` fail-closed 停止ログ〉。`1272-` プレフィックスによりファイル名衝突なし。詳細は同ドキュメント §5.11）も同居する
    │   ├── logs/metal-bench-robust-stats-1266/ # `metal-bench-noise-protocol.md` §8（ロバスト統計候補の再適用。イシュー #1266）向けの再集計スクリプト（`reapply.py`）・決定的出力（`reapply.md`）・README。新規計測なし（既存 #1255／#1186／#1187 ログの再集計のみ。env_info なし）
    │   ├── logs/metal-bench-robust-stats-1485/ # ロバスト統計の採用決定（併記のみ・置換なし。イシュー #1485）を受けた M4 Max 実機 1 run（`gemm_transpose_route_ab_bench --phase1-only`）の実行ログ・`within_gate` 判定不変の機械確認結果・`#[ignore]` 群非後退確認・env_info（内部ホスト名は含めない）
    │   ├── metal-gemm-n4096-kernel-gap.md # N=4096 カーネル純境界の candle 比ギャップ（約 9.9 対 13.17 TFLOPS）縮小調査。`MTLComputePipelineState` 反射値によるレジスタ圧仮説（H1）の検証（占有率上限に不足なし・反射値レベルでは非支持）・MLX steel classic 未収録構成 `(32,64,16,1,2)`（`CANDIDATES[8]`）の追加測定（劣後のため不採用・選択ロジック変更なし）。M4 Max 実機実測 2 run（イシュー #1143）。§7 で E1（`gemm_simdgroup_tiled` アキュムレータ系ループへの loop unroll pragma）軽量実験を追記: `acc_rows*acc_cols>=16` の候補（cand0/4/8）は全計測形状（512〜4096）で 5 回計測中央値 約 2.1〜14.2 倍改善する有効性を確認した一方、本番 `dispatch_auto` 経路は N=4096 のみ非後退（約 +11.5%）で N=512/1024/2048 では約 15〜35% 後退することが判明（PR #1204 codex-review 指摘。§7.6a）したため、**本番結線（`gemm.metal` への pragma 付与）は撤回**（§7.7a。`tile::select` 自体は不変。実験自体の知見・実測記録は保持）。M4 Max 実機実測（イシュー #1188）。§7.9 で条件付き gating（`UNROLL_ACC_ENABLED` function constant 分岐によるループ本体複製。acc 積 >= 16 の候補のみ unroll 版を選択）を実装し AC-1/AC-3 を自己検証（Linux。実機 bit 一致テストは #1284 へ引き継ぎ。イシュー #1282）。§8 で E2（タイル候補ごとにアキュムレータ配列を厳密サイズ化するソーステキスト特殊化経路。`crate::spec_source`・`MetalGemm::new_with_source_specialization`）を試作し、function constant 経路との bit 一致を全 9 候補×N=512〜4096 で M4 Max 実機実測により自己検証（本番既定 `SOURCE_SPECIALIZATION_ENABLED=false` は不変。性能実測・結線判断は #1289／#1302 へ引き継ぎ。イシュー #1288）。§10 で E3（フラグメントロード方式候補。5 候補: `tgp-k1`〈本番既定〉・`tgp-k2`・`device-legacy`・`device-hoisted-k1`・`device-hoisted-k2`）の N=1024/2048/4096 純カーネル時間を M4 Max 実機で 5 回計測（高負荷共有環境下）: 本番既定 `tgp-k1` が全 N で最速で他 4 候補は全て組み込み不可と判定・`tile::FRAG_LOAD_CONFIG` 不変維持（採用候補なし。イシュー #1295）。§7.10 で本番 `dispatch_auto` 経路の正方 4 形状（N=512〜4096）＋補助 2 形状（`CANDIDATES[0]` 到達）を M4 Max 実機で 6 試行計測: フェーズ 1 安定性ゲートが低負荷時を含め 6 試行すべて不成立のため**判定不可**・`UNROLL_ACC_ENABLED=false` 維持（機構は revert せず）。実測値自体は一貫して正方形状が後退なし〈中央値 1.01〜1.04 倍〉・補助形状は 5.4〜7.3 倍改善という方向性を示すが、フェーズ 1 未成立のため結線根拠としない。#1282 の実機 `#[ignore]` bit 一致テスト 3 件・parity 群は全 pass（実機初回実行。イシュー #1284）。§11 で E4（協調ロードレイアウト候補。6 候補: `L0-P4`〈本番既定〉・`L0-P0`・`L0-P8`・`L1-P0`・`L1-P4`・`L1-P8`）の N=1024/2048/4096 純カーネル時間を M4 Max 実機で 5 回計測（高負荷共有環境下）: 本番既定 `L0-P4` を安定して上回る候補は確認できず（`RowStrided` 系〈L1〉は N=4096 で符号一貫の 5.6〜15.4% 後退・`L0-P0`／`L0-P8` は改善方向のシグナルがあるが N=2048/4096 で run 間の符号が反転し確証に至らず）**組み込み対象なし**・`tile::COOP_LOAD_CONFIG` 不変維持（採用候補なし。イシュー #1300）。§12 で E6（内部タイル direct-load／端タイル staged 2 クラス経路。`tile::TileClassMode::Split`）の候補 0/4/5/8×N=1024/2048/4096（AC 形状。候補 0/4/8 は Split が 0.43〜0.85 倍改善する一方候補 5 は 2.3〜2.7 倍後退という候補依存の符号反転を確認。改善は `bk` 依存ではなく候補 0/4/8 の legacy ベースライン自体が本番選択構成より約 7〜8 倍遅いことに起因すると推定）に加え、本番 `select_for_device` 選択構成×N=512/1024/2048/4096 を直接計測（N=512 の選択構成は候補 5 自体）: 本番選択構成では N=2048/4096 で Split が一貫して 2.3〜3.1 倍後退（5/5 run 符号一貫）・N=1024 もおおむね後退方向のため**組み込み不可（REJECT）**・`tile::TILE_CLASS_MODE=Legacy` 不変維持（M4 Max 実機実測。イシュー #1328）。#1329 で §13（E7 候補 `CANDIDATES[9]`＝64×64×32 の追加・反射値・全形状 × 転置 4 種 parity 0 fail・`select` 非変更）を追記。#1330 で §14（E7 実測 — `CANDIDATES[9]`〈bk=32〉vs `CANDIDATES[0]`〈bk=16〉の A 系列・vs 本番選択構成の B 系列を M4 Max 実機で 5 回計測中央値比較: A 系列は全 N で 10.8〜15.8% 後退〈5/5 run 符号一貫〉・B 系列は全帯域〈N=512〜4096〉で 4.5〜7.6 倍後退〈20/20 反復符号一貫〉のため**組み込み不可（REJECT）**・`tile::select` 系関数不変維持〈共有負荷下だが符号完全一致のため判定不可とはしない〉）を追記。#1331 で §15（E8 候補 `CANDIDATES[10]`＝128×64×16 の追加・反射値・全形状 × 転置 4 種 parity 0 fail・f16 版 SMEM 超過〈40064 バイト〉により本候補のみ f16 経路が構造的に使用不可〈`f16_tiled_fits_standard_limit`／`f16_tiled_candidates` によるドリフト検出込み〉・`select` 非変更）を追記。#1332 で §16（E8 実測 — `CANDIDATES[10]`〈128×64×16〉vs `CANDIDATES[0]`／`[3]` の A 系列・vs 本番選択構成の B 系列を M4 Max 実機で 5 回計測中央値比較: A 系列は `[0]` に対し全 N で 1.65〜2.40 倍後退・`[3]` に対し全 N で 10.5〜14.2 倍後退〈5/5 run 符号一貫〉・B 系列は全帯域〈N=512〜4096〉で 9.7〜13.0 倍後退〈20/20 反復符号一貫〉のため**組み込み不可（REJECT）**・`tile::select` 系関数不変維持〈共有負荷下だが符号完全一致のため判定不可とはしない〉）を追記。#1304 で §18（E2〜E4 はいずれも実機実測で REJECT 済みにつき`tile::select` 組み込み対象なし・本番選択構成〈`select_for_device`〉の全形状 × NN/NT/TN/TT parity・既存 bit 一致テスト群を M4 Max 実機で非後退確認）を追記。本番コード無変更（イシュー #1304）。#1306 で §19（候補表更新後の framework-compare gemm metal 結線前後 A/B。字義通りの結線前後ではなく v0.7.0 ↔ HEAD 非後退確認〈#1304 が `tile::select` 系を不変にしたため〉と明記した上で、N=512/1024/2048/4096 × fresh/reuse の全 8 セルが非後退・checksum 完全一致であることを M4 Max 実機実測で確認し、本番既定〈`select_for_device`〉を確定。コード変更なし）を追記
    │   ├── logs/metal-gemm-unroll-acc-ab-1284/ # 上記 6 試行の実行ログ・env_info・uptime・pmset・実機テストログ（内部ホスト名は含めない。イシュー #1284）
    │   ├── logs/metal-gemm-e7-candidate-1329/ # E7 候補 `CANDIDATES[9]`（64×64×32 wm2wn2）の反射値・parity・回帰確認の実行ログ・env_info（内部ホスト名は含めない。イシュー #1329）
    │   ├── logs/metal-gemm-e8-candidate-1331/ # E8 候補 `CANDIDATES[10]`（128×64×16 wm2wn2）の反射値・parity・回帰確認の実行ログ・env_info（内部ホスト名は含めない。イシュー #1331）
    │   ├── logs/metal-gemm-e8-bm128-ab-1332/ # E8 実測 — `CANDIDATES[10]`（128×64×16）純カーネル時間比較の A/B 系列 5 プロセス起動実行ログ・aggregate.md・env_info・uptime・pmset（内部ホスト名は含めない。イシュー #1332）
    │   ├── metal-gemm-candle-gate-remeasurement.md # Metal GEMM N=1024/2048/4096 reuse の candle 比 5 回計測中央値再計測・#1037 ゲート達成判定の確定記録（正式系列〈fandhe-ai =0.6.0〉・参考系列〈#1167/#1168 反映後 HEAD〉の 2 系列併記。いずれも未達成。M4 Max 実機実測。イシュー #1147。#1185 で正式系列 `fandhe-ai =0.7.0` を 2026-09-06 に再計測し §11 追記: 0.836／0.638／0.509 倍〈共有負荷下〉で #1037 未達成を確定。#1337 で借用ビュー readout 切替前後を §12 追記: 共有負荷下・全 3 形状で後退したが片方向の負荷差と切り分けられておらず正式判定は不変）。#1438 で §13 追記: 同一 facade ソースで揃えた before/after を M4 Max 実機再計測し、全 3 形状非後退・checksum 完全一致を確認（§12 の facade ソース不一致の交絡は解消したが、before/after が同一マシン上で連続実行され負荷変動と readout 切替の効果を分離できていないため参考結果〈暫定・ADOPT 保留〉。正式判定は §11 のまま不変。§13.5）。#1309 で §14 追記: #1269 Phase 3（#1280・#1302・#1308・#1334・#1368 反映後）の正式系列 `fandhe-ai =0.7.0` を 2026-09-09 に再計測し §11 の未達成判定を再確認（0.700／0.969／0.710 倍・共有負荷下・全 run parity 0 fail）。差分はコード変更ではなく計測時点間の共有負荷差によるノイズと判断。参考系列は負荷ゲート不通過により未計測のまま後続へ引き継ぎ。#1477 で §15 追記: `--readout <legacy|borrowed>`（同一バイナリの runtime override）による legacy/borrowed の run 単位 interleave 再計測を実装し、§13.5 が暫定・ADOPT 保留とした判定の解消（`readout_uses_borrowed_view` の Metal 分岐の採否確定）を目的とする事前登録判定規則を確定（正式系列 `=0.7.0` の #1037 判定は不変）。1 回目の実機実測試行は専有ゲート（1 分 load average < 4.0 を 2 回連続）が最大 4 試行以内に成立せず undetermined（計測未開始のまま終了。再試行は専有環境が確保できるタイミングで別途実施）。#1490 で正式系列 `fandhe-ai =0.8.0` を 2026-09-10 に再計測し §16 追記: N=1024 0.743 倍（未達）・N=2048 1.002 倍（**達成。正式系列として初めて #1037 の形状別条件を満たした**）・N=4096 0.634 倍（未達）。v0.7.0 → v0.8.0 間で本番 NN 正方 GEMM reuse 経路への変更はなく（split-K opt-in 実装・結線は `SPLIT_K_NUMERIC_CONTRACT_APPROVED=false` により対象形状へ非到達）、N=2048 の改善は §11／§14 との比較よりコード変更に帰属できない計測ノイズと判断。旧 #1037 の「3 形状すべて」という受け入れ条件は依然として未達成のまま（#1518 で §16.2／§16.8 に HEAD 時点〈#1513/#1516 で数値契約解除・定数ゲート付き結線済み・既定 OFF〉の補足を追記。対象形状は非到達のため帰属判断は不変）。#1520 で 2026-09-11 に M4 Max を record_only で実測し REJECT（N=1024 fresh/reuse が 1.211／1.111 で後退・checksum 全セル一致）→ legacy フォールバック維持（§17。§13.5 暫定・ADOPT 保留は REJECT 確定）。#1521 で split-K 本番結線（#1527・#1530）により初めて `v0.8.0 ↔ origin/main` の Metal 計測経路 diff が非ゼロになったことを受け、対照系列・参考系列（split-K 結線後 HEAD）2 系列の再計測スキャフォールド・事前登録判定規則を §18 として整備（実測は Mac セッションへ申し送り）。#1521 で 2026-09-11 に M4 Max を record_only で実測（§18）: A・B とも 3 形状未達・parity 0 fail・帰属は 3 形状とも負荷差（ノイズ帯）・正式判定は不変
    │   ├── logs/metal-gemm-candle-gate-1147/ # 上記の実行ログ・env_info（内部ホスト名は含めない。イシュー #1147）
    │   ├── logs/metal-gemm-readout-interleave-1520/ # `--readout <legacy|borrowed>` interleave 再計測（イシュー #1520・2 回目・record_only モード）の M4 Max 実機実測（2026-09-11・legacy REJECT・checksum 全セル一致）ログ・JSONL・compare 出力・env_info。REJECT（N=1024 fresh/reuse が 1.211／1.111 で後退。legacy フォールバック維持・§13.5 暫定判定は確定）
    │   ├── logs/metal-gemm-candle-gate-head-1521/ # split-K 結線後 HEAD（参考系列）の再計測スキャフォールド（`orchestrate_m4max.sh`〈`--dry-run` あり。record_only・A→B 固定順〉・`attribute.py`〈`--self-test` あり〉・`attribution.md`・`README.md`・`env_info.txt`・`diff_v0.8.0_origin-main_metal_path.txt`）。2026-09-11 に実測完了（両系列の run ログ・JSONL・compare-A/B・attribution-result を同梱。イシュー #1521）
    │   ├── metal-gemm-reuse-phase-breakdown.md # Metal GEMM reuse 計測境界を upload／encode／commit_wait／readback／host_copy でフェーズ分解（CUDA #1182 の Metal 版）。CUDA とは非対称な結論: N=1024/2048 は matmul 単体が candle fresh とほぼ同等（0.90〜0.99倍）だが N=4096 は matmul 単体が candle fresh より1.52倍遅く、reuse 計測境界の再定義では候補比未達を解消できない構造的ギャップと判明。M4 Max 実機実測。イシュー #1189。#1276 で GPU タイムスタンプ変種 `kernel_gpu`（`MetalContext::synchronize_with_gpu_timestamps`。本番 `synchronize()` は no-op オブザーバで不変）を診断経路へ実装し自己検証テスト green（M4 Max 実機実測）。#1277 で N=1024/2048/4096 の `kernel_gpu` 5 プロセス起動中央値・N=4096 ギャップのカーネル専有分の割合（約 27.6%）・Phase 2 分母表を §11 に追記（M4 Max 実機実測）
    │   ├── logs/metal-gemm-reuse-phase-1189/ # 上記の実行ログ・env_info（内部ホスト名は含めない。イシュー #1189）
    │   ├── logs/metal-gemm-reuse-phase-1277/ # 上記の実行ログ・env_info・uptime サンプリング（内部ホスト名は含めない。イシュー #1277）
    │   ├── cpu-gemm-candle-gate-remeasurement.md # CPU GEMM N=512/1024/2048 reuse の candle 比 5 回計測中央値再計測・#1117 ゲート判定の確定記録（単一系列〈fandhe-ai =0.6.0〉。CPU GEMM 本番経路が v0.6.0 と HEAD で同一のため参考系列は計測せず。DGX Spark GB10〈Grace CPU〉・Apple M4 Max とも実機実測完了。両実機 N=512/1024 未達・DGX N=2048 は candle 側要素誤差超過により判定不能〈5 run で完全に決定的に再現〉・M4 Max N=2048 は未達。未達原因分析〈計測境界固定費・並列化の非単調性・マイクロカーネル効率・packing〉を含む。イシュー #1148。#1185 で正式系列 `fandhe-ai =0.7.0` を 2026-09-06 に両実機で再計測し §12 追記: DGX 0.810／0.786／判定不能・M4 Max 0.922／0.778／0.829 倍で #1117 未達成を確定。#1364 で既定スレッド数の大コア限定〈#1363〉on/off を両実機比較し §13 追記: M4 Max 0.80〜0.91 倍〈改善〉・DGX 1.19〜4.34 倍〈`cpu_capacity` 誤検出による重大な後退〉で REJECT・`BIG_CORE_LIMIT_ENABLED=false` へ差し戻し確定。#1337 で借用ビュー readout 切替前後を両実機で §15 追記: DGX は非後退だが未達のまま・M4 Max は達成見込み〈片方向負荷差あり・確度限定的〉で正式判定は不変。#1292 で reuse 計測境界のフェーズ分解を両実機 5 回計測中央値実測し §16 追記: §8.1 の固定費推定を Layer A（`matmul`／`host_copy`／`checksum`／`iter_total`）・Layer B（`alloc_c`／`kernel`／`ops_gemm`／`tape_matmul` 等）へ内訳分解・DGX N=2048 の `alloc_c` 異常〈2.8〜3.0 ms〉を発見・ハーネス診断コスト対本番経路固定費の区分と削減優先順位〈`alloc_c` 最有力〉を確定）。#1305 で専有環境の RAYON_NUM_THREADS スイープを両実機 5 回計測中央値再実測し §17 追記: DGX Spark GB10 の N=1024 非単調性を taskset pin 実験（大コア pin で T=8 以上へ回復・小コア pin で顕著に劣化）で H1（異種コア由来）と確定・Apple M4 Max は専有ゲート不通過のため undetermined のまま記録・#1307（2D 動的分配設計）は N=1024 近傍・大コア数前後のスレッド数を優先対象として続行する前提を整理。#1312 で `TwoDDynamic` vs `RowPanel` 両実機 A/B を実施し §18 追記: DGX は全形状で `RowPanel` を 1.09〜1.80 倍上回ったが M4 Max 専有ゲート不通過のため最終判定 undetermined（結線せず #1313 へ記録のみ引き継ぎ）・DGX 単独の非単調性（T10/T8）は「残存」と判定。#1262 で承認済み契約下の DGX N=2048 再計測を §19 追記: 「判定不能」が解消して確定判定（未達・0.950 倍）へ遷移・N=512/1024 は誤差範囲内で再現・Apple M4 Max は N=2048 が元々 0 fail のため対象外。#1301 で出力並列ゼロ埋め（#1299）on/off を両実機実測し §20 追記: 両実機・全形状で非後退（DGX N=2048 alloc_c 約 48% 削減）を確認しいったん `GEMM_OUTPUT_PARALLEL_ZERO_MIN_ELEMS` を `2 << 20` へ有効化したが、事前宣言した規則 4（candle 比の非後退）が緩和なしでは 6 セル中 3 セルで不成立だった点を PR #1448 の codex-review が指摘したため `usize::MAX` へ差し戻し・規則 4 改定版（§20.1a）を事前登録規則として固定した独立の再計測（§20.6）へ本番採用可否を引き継いだ。#1313 で `TwoDDynamic`（2D 動的分配）を §21 追記: M4 Max 専有ゲート付き Phase 0 再計測で ADOPT 確定・本番結線し framework-compare gemm cpu 両実機・全 12 セルが非後退（改善方向）であることを確認（candle 比ゲート自体の判定は不変）。#1321（#1283 Phase 4）で Phase 1〜3 結線後（実質 `TwoDDynamic` のみ）の両実機ゲート再判定を §22 追記: 正式系列（`fandhe-ai =0.7.0` ピン）は両実機・全形状で未達成のまま不変（DGX N=2048 は §19 確定判定を再確認）だが、参考系列（origin/main HEAD）では Apple M4 Max が全 3 形状・DGX Spark GB10 が N=2048（1.562 倍）で初めて candle 比ゲート達成を記録（ピン未更新のため正式判定は不変） #1438 で §23 追記: 同一 facade ソースで揃えた before/after を両実機再計測し、判定対象の reuse 全 6 セル非後退・checksum 完全一致を確認（ADOPT。§15 の確度限定的だった留保を解消。正式判定は §12／§19 のまま不変。fresh は参考記録〈§22.2a〉で DGX 全形状改善だが M4 Max N=512/1024 は 1.0115／1.0028 と僅かに後退方向・計測ノイズかは本記録のみでは確定できない）。#1481 で §20.6 の独立再計測を実施し verdict=REJECT を確定（§20.7）・#1482 で `usize::MAX` を確定既定化。#1488 で正式系列 `fandhe-ai =0.8.0` を 2026-09-10 に両実機で再計測し §24 追記: DGX Spark GB10 は N=2048（1.400 倍）で正式系列として初めて candle 比ゲート達成を記録（N=512/1024 は未達）。M4 Max は専有ゲートの試行を当初計 3 回実施し（1 回目は誤って固定間隔で実行したため破棄・2 回目は是正済みバックオフ系列だが 2 回目確認が僅かに不合格で不成立・3 回目で再度 attempt=1 から起動し通過。N=512〈1.159 倍〉・N=1024〈0.854 倍〉・N=2048〈1.075 倍〉）、この 3 回目を当初正式値としていたが、プロセス再起動により試行カウンタと約 30 分上限がリセットされてしまい、事前宣言した終了条件（最大 10 試行・約 30 分上限で不成立なら undetermined）に反していたため、PR #1506 の codex-review 指摘を受け参考記録へ降格。プロセス再起動をまたいで通用する経過時間上限を実装した是正版オーケストレーションで独立再計測したが、事前宣言した約 30 分上限内に専有ゲートが成立せず `verdict=undetermined` に終わり、M4 Max の正式値は #1488／#1506 時点で未確定のまま残る（詳細は同ドキュメント §24.1／§24.10）。#1522 で record_only 版オーケストレーション（専有ゲート opt-out。ルート #1519 のユーザー指示）を整備し、2026-09-11 に M4 Max を実測して **正式値 N=512 達成・N=1024/2048 未達**（parity 全 0 fail）として§24.10 の undetermined を解消（§24.11.1）
    │   ├── cpu-gemm-default-thread-limit.md # `gemm_blis` 並列 GEMM の既定並列度を大コア数へ限定するプラットフォーム判定（macOS `hw.perflevel0.logicalcpu`／Linux sysfs `cpu_capacity`。`crates/backend-cpu/src/thread_limit.rs`）の設計・判定不能時フォールバック契約・`RAYON_NUM_THREADS` との関係・§8.2 の非単調性仮説の実装（イシュー #1363・親 #1362・祖 #1361）。#1364 で両実機実測・REJECT（不採用）を §6 に確定記録: DGX Spark GB10 の `cpu_capacity` sysfs 値が `lscpu` の X925×10+A725×10 構成と一致しない 5 段階の非一様分布で `detected_big_cores=Some(1)`（実質シングルスレッド化）と誤検出し 1.19〜4.34 倍の重大な後退・M4 Max は 0.80〜0.91 倍の改善を確認したが両実機総合で不採用・`BIG_CORE_LIMIT_ENABLED` を `false` へ差し戻し済み
    │   ├── cpu-gemm-gb10-affinity-ab.md # GB10 小形状 GEMM 大コア OS affinity（`crate::gb10_affinity`。設計 `docs/backend-cpu-gb10-affinity-design.md`）の A/B 実測記入欄。事前登録判定規則（train/infer size=64 対象 4 セル・gemm N=256〜4096 ガードセル 10 個・5 run 中央値・checksum 完全一致・非後退 `ratio<=1.00`）を固定。本エージェント実行環境に DGX Spark GB10 実機への到達手段が無いため実測は未実施（既定 `GB10_AFFINITY_ENABLED=false` のまま。イシュー #1576）
    │   ├── logs/cpu-gemm-gb10-affinity-1576/ # 上記の実測ログ置き場（本 PR 時点では未生成。README に保存すべきファイル一覧のみ記載。イシュー #1576）
    │   ├── logs/cpu-gemm-candle-gate-1148/ # 上記の実行ログ・env_info・RAYON_NUM_THREADS スイープログ（内部ホスト名は含めない。イシュー #1148）
    │   ├── logs/cpu-gemm-thread-limit-1364/ # 既定スレッド数の大コア限定（#1363）on/off 両実機比較の env_info・`thread_limit_report()` 生値ログ・実行ログ（内部ホスト名は含めない。イシュー #1364）
    │   ├── logs/cpu-gemm-rayon-sweep-1305/ # 専有環境での RAYON_NUM_THREADS スイープ再実測（主スイープ・DGX 限定 taskset 補助軸 A・制御形状 N=1920 補助軸 B）の生ログ・env_info・gate/uptime 記録・集計スクリプト（内部ホスト名は含めない。イシュー #1305）
    │   ├── logs/gemm-candle-gate-0.7.0-1185/ # 正式系列 `fandhe-ai =0.7.0` の GEMM candle 比ゲート再計測（CUDA / Metal / CPU・DGX Spark GB10 / M4 Max）の実行ログ・env_info（内部ホスト名は含めない。イシュー #1185）
    │   ├── logs/cpu-gemm-candle-gate-1262/ # 上記 §19（承認済み契約下の DGX N=2048 再計測）の実行ログ・判定表出力・env_info（内部ホスト名は含めない。イシュー #1262）
    │   ├── logs/cpu-gemm-candle-gate-1321/ # 上記 §22（Phase 1〜3 結線後の両実機ゲート再判定）の実行ログ・判定表出力・env_info・diff・attribution・gate/uptime ログ（内部ホスト名は含めない。イシュー #1321）
    │   ├── logs/cpu-gemm-candle-gate-0.8.0-1488/ # 上記 §24（正式系列 `fandhe-ai =0.8.0` 再計測）の実行ログ・判定表出力・env_info・diff・attribution・gate/uptime ログ（M4 Max は誤った固定間隔の試行 1・是正済みバックオフ系列だが 2 回目確認が僅かに不合格で不成立に終わった試行 2・同系列を再起動し専有ゲートを通過した試行 3〈参考値へ降格〉の計 3 回に加え、同配下 `m4max-redo-pr1506/` に PR #1506 の是正版オーケストレーション〈session-start スタンプ・累積試行数／バックオフ状態の fail-closed 永続化・待機時間の非短縮・待機後の期限再確認〉による独立再計測〈同一セッション累積 12 試行・専有ゲート不成立・`verdict=undetermined`〉を記録）（内部ホスト名は含めない。イシュー #1488）
    │   ├── logs/cpu-gemm-candle-gate-0.8.0-m4max-1522/ # 専有ゲート opt-out（`GEMM_GATE_LOAD_GATE_MODE=record_only`。ルート #1519 のユーザー指示）版オーケストレーション（`orchestrate_m4max.sh`。派生元は同配下 `cpu-gemm-candle-gate-0.8.0-1488/orchestrate_m4max.sh`）・事前登録判定規則・env_info・`aggregate_uptime.py`（load1 集計）。2026-09-11 に M4 Max を実測完了（ログ・JSONL・compare 出力を同梱）。正式値 N=512 達成・N=1024/2048 未達（parity 0 fail。§24.11.1。共有負荷下 load1=4.92。単一ラン・ワンショット規則適用）（内部ホスト名は含めない。イシュー #1522）
    │   ├── logs/cuda-graph-step-ab-1350/ # CUDA Graph step capture（#1349・`--graph`）3 状態 A/B（Phase 0 実機ゲート 3 種・150 run 本体・`compare_graph_ab.md`）の実行ログ・JSONL・env_info（内部ホスト名は含めない。イシュー #1350）
    │   ├── logs/cuda-graph-step-grad-1480/ # CUDA Graph step capture の bit 同一性比較へ各 step の重み勾配生値（`Tape::param_grads_to_host`。イシュー #1479 で追加した公開 API）を追加した GB10 実機実測（4782 行すべて bit 同一・`grad_readout_contract_on_cuda` pass 込み）の実行ログ・env_info（内部ホスト名は含めない。イシュー #1480）
    │   ├── logs/gemm-candle-gate-readout-1337/ # 借用ビュー readout（#1337）切替前後 3 バックエンド × 両実機の実行ログ・env_info・A/B 集計（内部ホスト名は含めない。イシュー #1337）
    │   ├── logs/metal-gemm-candle-gate-1309/ # Phase 3 反映後の Metal GEMM candle 比ゲート正式系列再計測（#1280・#1302・#1308・#1334・#1368 反映後の HEAD。イシュー #1309）の実行ログ・生データ・env_info・帰属表・負荷ゲートオーケストレーションスクリプト（内部ホスト名は含めない。参考系列は負荷ゲート不通過により未計測）
    │   ├── logs/metal-gemm-readout-interleave-1477/ # `--readout <legacy|borrowed>` interleave 再計測（イシュー #1477）の `run_ab_readout_metal.sh` 実行ログ・`gate.log`・`uptime.log`・`pmset_therm_{before,after}.txt`・`compare_readout_ab-<label>.md`・env_info（内部ホスト名は含めない。専有ゲート未通過時はその試行ログのみ）
    │   ├── logs/metal-gemm-candle-gate-0.8.0-1490/ # 上記 §16（正式系列 `fandhe-ai =0.8.0` 再計測）の実行ログ・判定表出力・専有ゲートログ・prebuild ログ・`orchestrate_m4max.sh`・env_info（内部ホスト名は含めない。イシュー #1490）
    │   ├── logs/gemm-candle-gate-readout-default-1438/ # 借用ビュー readout の既定経路化（旧 host-view-readout cargo feature 撤去。#1438）before/after 3 バックエンド × 両実機の実行ログ・env_info・A/B 集計・candle 比ゲート判定（同一 facade ソースで揃え #1337 の交絡を解消・判定対象の reuse 全セル非後退・checksum 完全一致。CPU の fresh 参考行は M4 Max N=512/1024 が僅かに後退方向。内部ホスト名は含めない。イシュー #1438）
    │   ├── train-backward-gemm-wiring.md # VJP の `matmul_vjp`／`Op::LinearResident.d_weight` を `eval::matmul`（ホスト scalar 参照実装）から `BackendOps::gemm`（forward と同じ CPU BLIS／CUDA／Metal カーネル）へ切替（#1211）。CPU 実測（Apple M4 Max。before/after × fresh/reuse の全 4 系列とも厳密 5 run 中央値）で backward が fresh 11.60 倍・reuse 8.86 倍、step_total が fresh 8.71 倍・reuse 5.63 倍改善し ADOPT と確定。Metal 参考計測・CUDA 実測は後続 #1214/#1215 へ引き継ぎ
    │   ├── logs/train-backward-gemm-wiring-1211/ # 上記の before/after JSONL・env_info（内部ホスト名は含めない。イシュー #1211）
    │   ├── cpu-gemm-vjp-transposed-entry.md # CPU BLIS GEMM に VJP 専用 NT/TN 2 パターン入口（`pack_a_from_transposed`／`pack_b_from_transposed`・`gemm_blis_parallel_nt`／`_tn`）を追加し、`CpuBackendOps::gemm`／`gemm_resident_lhs` の転置オペランド再パックを一般 stride 化なしで解消（#1213）。bit 完全一致契約・M4 Max 実機実測（補助 A/B 全形状 1.07〜3.10 倍改善）・ADOPT 判断を記録。TT・一般 stride・CUDA（#1214）・Metal（#1215）は対象外
    │   ├── logs/cpu-gemm-vjp-transposed-entry-1213/ # 上記の補助 A/B 生ログ・env_info（内部ホスト名は含めない。イシュー #1213）
    │   ├── cuda-gemm-vjp-transposed-entry.md # CUDA GEMM に VJP 専用 NT/TN 転置入口（GPU 側 smem 転置カーネル `transpose_smem_f32` → 既存 NN GEMM カーネル方式。`CudaGemm::run_tiled_f32_nt`／`_tn`／`launch_tiled_f32_resident_nt`）を追加し、`CudaBackendOps::gemm_fp32_strict_impl`／`gemm_resident_lhs` の転置オペランド再パックを解消（#1214）。bit 完全一致契約・GPU 非依存単体テストは実装完了・GB10 実機実測は本エージェント実行環境に CUDA 実機なしのため未実施のまま記入欄を残す（issue 本文「再分解」節で (b) として引き継ぎ）。TT・一般 stride・TF32 opt-in 経路・Metal（#1215）は対象外
    │   ├── metal-gemm-vjp-transposed-entry.md # Metal GEMM に VJP 専用 NT/TN strided 入口（`layout::classify_2d` 分類済み `MatrixLayout` → `gemm::MetalGemm::dispatch_strided_bias_act_prepared`。`gemm_resident_lhs`〈#1040〉が確立した classic strided カーネル入口を再利用）を追加し、`MetalBackendOps::gemm` の転置オペランド再パックを NT/TN 限定で解消（#1215）。CPU／CUDA と異なりカーネル自体が変わるため REQ-2 統一複合判定（bit 一致ではない）を受け入れ契約とする。M4 Max 実機実測（補助 A/B・train phases フル A/B）で ADOPT 確定（train phases: backward fresh 1.649×・reuse 1.200×、step_total fresh 1.323×・reuse 1.109×）。TT・一般 stride・`d_input` デバイス直接計算化は対象外
    │   ├── logs/metal-gemm-vjp-transposed-entry-1215/ # 上記の補助 A/B・train phases フル A/B 生ログ・env_info（内部ホスト名は含めない。イシュー #1215）
    │   ├── linear-forward-device-gpu.md # `BackendOps::linear_forward_device`（`a`／`w`／`bias`／戻り値すべて `DeviceBuffer` 常駐の推論チェーン用非破壊拡張。`docs/inference-forward-fixed-cost-design.md` §3.2「段階 B」）を CUDA／Metal に実装（`gemm_resident_rhs_act` と同一の融合カーネルを再利用・CUDA は世代検査対象に `a` を追加）。M4 Max 実機実測完了（2 層チェーン before/after 5 回計測、旧経路比 1.29〜1.81 倍改善）・CUDA は本エージェント実行環境に実機なしのため未実測明記。facade／autodiff への結線（Phase 2）は独立変更単位として本 PR では見送り、#1217 等へ引き継ぎ（#1216）
    │   ├── train-resident-grad-device-update.md # reuse 経路の `Op::LinearResident` weight 勾配をデバイス常駐 staging（`DeviceParamStore::GradStaging`）へ backward 内で直接書き込み、`device_update` フェーズの H2D を CPU バックエンド限定で排除（#1212）。#1555 で Metal 実装済み（NT/TN は encode-only で staging へ直接書き込み・それ以外の形状は `gemm_fp32_strict` → `upload_into` フォールバックで形状理由の `Unsupported` は返さない〈PR #1556 codex-review P1 是正〉・reuse step_total 0.878 倍・bit 同一・カウンタ 11/9/9）。CPU 実測（Apple M4 Max）で `step()` update フェーズ単体が約 1.4〜1.46 倍改善・全体は計測ノイズ内で非後退。§6 で #1559 の CUDA 実装記録を追記（#1214 の GPU 側 smem 転置カーネル再利用による `gemm::CudaGemm::launch_tiled_f32_nt_into`／`_tn_into`〈`CudaArgMut::View`／`UnifiedView` 新設〉。GB10 実機での bit 同一検証・性能 A/B は #1560 へ引き継ぎ・本エージェント実行環境に CUDA 実機なしのため未実測明記）。§4 に「ホスト読み出し公開 API は #1479 で追加」を追記。§7 で #1560 の実測スキャフォールド（`docs/perf/logs/train-resident-grad-cuda-1560/`・`run_ab_resident_grad_cuda.sh`。事前登録判定規則・記入欄のみ・本 PR 実行環境に CUDA 実機到達手段がなく未実測明記）を追記。#1563 で §5.2 の誤記（テスト所在を `crates/facade/tests/mnist_scale_train_reuse_bench.rs` へ訂正）・§5.6（d_input GEMM 同期境界のスコープ更新）・§8（層内合流の前後比較記入欄）を追記。§9 で #1566 の bias 勾配デバイス常駐化（採用案 A′。`docs/backend-metal-command-batching-design.md` §10.7 参照）を追記（`reduce_to_shape` との bit 一致設計・`.claude/rules/coding-rust.md` の勾配長軸縮約 `f64` アキュムレータ方針との不整合は未解決のまま引き継ぎ・Mac 実機実測は未実施）。PR #1659 の codex-review P1 指摘（補償なし f32 逐次和が規約に反する）を受け、2026-09-12 ユーザー承認「選択肢 A」により数値方式を f64 相当（ホスト f64 アキュムレータ・Metal は当初 Neumaier 改良版 Kahan 補償和のみ）へ統一（`docs/backend-metal-command-batching-design.md` §10.8）。bias 勾配の before/after 比較は REQ-2 統一複合判定で行う。同 codex-review の追加指摘を受け、`crates/autodiff/src/grad.rs`（`Op::LinearResident` の resident 非対応バックエンドフォールバック・`Op::LinearAct`）の bias 縮約も同じ f64 ヘルパ（`grad::reduce_bias_grad` 新設）へ統一（汎用 `reduce_to_shape`・`Op::Add` 等 bias 以外は不変。`docs/backend-metal-command-batching-design.md` §10.9）。weight 勾配・loss は単発 backward では bit 同一契約のまま不変だが、多 step train reuse A/B では bias 乖離が SGD 更新経由で以降の step 全体へ伝播するため REQ-2 判定へ拡大する。§10.10 で Metal 実装形を精密化（単純 Neumaier のみでは有限入力の中間 overflow を防げないため scale 方式〈LAPACK SLASSQ 系の線形和版〉+ Neumaier 補償和へ変更。契約自体は不変）。#1665（origin/main 側 #1563。層内合流で d_weight encode を d_input 同期点より前へ移す）取り込み時（本コミット）に、`mnist_scale_train_reuse_bench.rs` 冒頭の #1566 doc comment（round1）が含んでいた「L1 は NN・分類不能」という根拠のない誤りを是正し、L1・L2 とも同一 NT パターンで分類されるため bias resident 化後もカウンタ 11/8/8 が不変という机上結論を確定。§10.11 で Metal `scale` を 2 のべき乗（`bias_pow2_floor`）へ限定する追加是正（codex-review 指摘: 任意実数 scale では相殺入力で丸め誤差が残る）を追記（Rust ホストモデル `gemm_bias_scale_sum_host_model.rs` で検証。契約は不変）。§10.12 で `Op::Add`（`nn::Linear` 既定 forward 経路 `LinearVars::forward` が経由）の bias パターンも `reduce_bias_grad` へ横展開（2026-09-12 ユーザー承認）を追記——`LinearVars::forward`（fresh・非融合）・`Op::LinearAct`（fused）・`Op::LinearResident`（reuse）いずれの経路でも bias 勾配の数値方式が揃う。回帰テスト `nn::linear::tests::linear_vars_forward_bias_grad_preserves_cancelling_contribution`・`add_path_bias_grad_matches_linear_act_bias_grad_bit_exact` を追加。2026-09-12 最終形: Metal カーネルを binary64 加算の 64bit 整数エミュレーションへ置換しホストと bit 完全一致（`docs/backend-metal-command-batching-design.md` §10.14）
    │   ├── logs/train-resident-grad-metal-1555/ # Metal `gemm_fp32_strict_into` 実装・reuse weight 勾配 staging 直接書き込みの M4 Max 実機実測（2026-09-12・record_only・共有負荷 load1 ≈ 7.9〜8.5）。bit ダンプ 4462 行完全一致・framework-compare train reuse step_total 0.8784 倍（5 run 改善一貫）・checksum 完全一致。フェーズ分解で backward 0.676 倍・device_update 同期点移動・step_total 0.905 倍。内部ホスト名は含めない。イシュー #1555
    │   ├── logs/metal-dinput-sync-1562/ # `d_input`（`gemm_resident_lhs`）同期境界の切り分け・定量化（`docs/backend-metal-command-batching-design.md` §7.3）の M4 Max 実機実測オーケストレーション（`orchestrate.sh`〈`--dry-run` あり〉・README・`env_info.txt.example`）。本エージェント実行環境に Apple Silicon 実機がないため実測ログ自体は未生成（記入欄のみ。イシュー #1562。親 #1557・#1561）
    │   ├── logs/metal-dinput-sync-1563/ # `d_input` 経路の同期境界回収の実装（層内合流。`docs/backend-metal-command-batching-design.md` §7.4）の M4 Max 実機実測オーケストレーション（`orchestrate.sh`〈`--dry-run` あり〉・README・`env_info.txt.example`。bit 同一確認・`#[ignore]` 群非後退・カウンタ実測・`scripts/bench/framework-compare/run_ab_dinput_sync_metal.sh` による A/B）。本エージェント実行環境に Apple Silicon 実機がないため実測ログ自体は未生成（記入欄のみ。イシュー #1563。親 #1557・#1561・#1562）
    │   ├── logs/train-resident-grad-cuda-1560/ # CUDA resident weight 勾配経路（#1559）の GB10 実機実測スキャフォールド（`orchestrate.sh`・`run_bitdump.sh`・`run_ignored_tests.sh`・README・事前登録判定規則・記入欄のみ。本イシューの実行環境に DGX Spark GB10 実機への到達手段がなく実測値は含まない。イシュー #1560）
    │   ├── infer-reuse-phase-breakdown.md # `bench-fandhe --task infer` への `--mode reuse`（`Sequential::predict_resident` 経由）・`--phases`（fresh/reuse 双方）追加（#1217）・`summarize.py` (c')/(c'') 節・M4 Max 実機初回実測（fresh→reuse 改善 cpu 1.47 倍・metal 1.05 倍。facade Phase 2〈#1216 の `linear_forward_device` 結線〉未実施のため metal 改善は限定的と明記）・DGX Spark GB10 記入欄（未実測明記）
    │   ├── cpu-infer-predict-profile.md # CPU 推論 `Sequential::predict`（tape 不要経路）のフェーズ分解実測（`CpuBackendOps::new()` は ZST でホットスポットでないと確認・L1 GEMM が支配的）・`Linear::forward_host_with_activation`（inherent メソッド）新設による `Linear`→`ReLU` 限定の `gemm_bias_act` 結線・bit 完全一致契約（`Module::forward_host` trait レベルは非融合のまま不変）・before/after 5 回計測（約 1.16 倍改善・ADOPT）。M4 Max 実機実測・DGX Spark GB10 は未実測明記（#1218）
    │   ├── cpu-gemm-default-thread-limit.md # `gemm_blis` 並列 GEMM の既定並列度を大コア数へ限定するプラットフォーム判定（macOS `hw.perflevel0.logicalcpu`／Linux sysfs `cpu_capacity`。`crates/backend-cpu/src/thread_limit.rs`）の設計・判定不能時フォールバック契約・`RAYON_NUM_THREADS` との関係・実機実測記入欄（未実測明記。性能上の採否は #1364 へ引き継ぐ。§8.2 の非単調性仮説の実装。イシュー #1363・親 #1362・祖 #1361）
    │   ├── metal-gemm-fine-barrier-ab.md # `gemm_simdgroup_tiled` staged 経路 kk ループへの `simdgroup_barrier(mem_none)` 細粒度同期挿入（`FINE_BARRIER_ENABLED` function constant。#809）の M4 Max 実機 A/B 計測記録。フェーズ 1 安定性セルフチェックが 4 試行・全サイズで不成立（他リポジトリの並走ビルドによる CPU 負荷競合と判断）のため**判定不可（undetermined）**・`FINE_BARRIER_ENABLED=false` 維持。数値契約（base/head bit 一致）は `tests/gemm_fine_barrier_bit_match.rs` で size 512〜4096 実機実測 PASS 済み。**#1280 で結線対象なしを確定・既定 `false` 維持・`tile.rs` doc comment 整合済み**（イシュー #1278）
    │   ├── logs/metal-gemm-fine-barrier-ab-1278/ # 上記の実行ログ・env_info（内部ホスト名は含めない。イシュー #1278）
    │   ├── metal-gemm-tgid-swizzle-ab.md # `gemm_simdgroup_tiled` の dispatch grid 走査順を threadgroup ID スウィズル（`SWIZZLE_ENABLED` function constant。#540・#795）へ変更した効果の M4 Max 実機 A/B 計測記録。フェーズ 1 安定性セルフチェックが 4 試行・ほぼ全サイズで不成立（同日 #1278 と同じ他リポジトリ並走ビルドによる CPU 負荷競合と判断）のため**判定不可（undetermined）**・`SWIZZLE_ENABLED=false` 維持。数値契約（base/head bit 一致）は `tests/gemm_swizzle_bit_match.rs` で size 512〜4096 実機実測 PASS 済み。**#1280 で結線対象なしを確定・既定 `false` 維持・`tile.rs` doc comment 整合済み**（イシュー #1279）
    │   ├── logs/metal-gemm-tgid-swizzle-ab-1279/ # 上記の実行ログ・env_info（内部ホスト名は含めない。イシュー #1279）
    │   ├── metal-gemm-frag-load-candidates.md # `gemm_simdgroup_tiled` のフラグメントロード方式候補（threadgroup 経由／device 直接・ロード粒度 K=1/2。`FRAG_LOAD_DEVICE_HOISTED`／`FRAG_LOAD_KSTEPS` function constant。5 候補）の実装・bit 一致自己検証（`MetalGemm::new_with_frag_load`。M4 Max 実機実測 T1〜T4 全 pass・既存 parity/bit-match 非後退確認済み）。性能実測・判定結果は #1295 で確定（`docs/perf/metal-gemm-n4096-kernel-gap.md` §10 参照。イシュー #1293。同ファイル §5 E3）
    │   ├── logs/metal-gemm-e3-frag-load-ab-1295/ # 上記 #1295 の 5 プロセス起動実行ログ・集計（`aggregate.md`）・env_info・uptime・pmset（内部ホスト名は含めない。イシュー #1295）
    │   ├── metal-gemm-coop-load-candidates.md # `gemm_simdgroup_tiled` の協調ロードレイアウト候補（スレッド→float4 グループ割当・行末パディング。`COOP_LOAD_LAYOUT`〈index 14〉／`TGP_PAD`〈既存 index 6 の 0/4/8 一般化〉function constant。6 候補）の実装・bit 一致自己検証（`MetalGemm::new_with_coop_load`）。Linux 実行可能な型検査・全単射モデル・証跡テストは green。**実機〈M4 Max〉での T1〜T6 は #1300 で実行済み（全 pass）**。バンク競合回避〈XOR swizzle〉軸は時間制約により未実装のまま index 15 を未割当で残す（**追記: #1327 で `TILE_CLASS` に割当済み。XOR swizzle 軸を実装する場合は index 16 以降**）。純カーネル時間比較・`tile::select` への組み込み判断は #1300 で確定（採用候補なし。#1302／#1304 は追加実装なしでクローズ可能。イシュー #1298。`docs/perf/metal-gemm-n4096-kernel-gap.md` §11 E4）
    │   ├── logs/metal-gemm-e4-coop-load-ab-1300/ # 上記 #1300 の 5 プロセス起動実行ログ・集計（`aggregate.md`）・env_info・uptime・pmset・T1〜T6 bit 一致ログ（内部ホスト名は含めない。イシュー #1300）
    │   ├── metal-gemm-tile-class-split.md # `gemm_simdgroup_tiled` の内部タイル direct load／端タイル staged 2 クラス経路（`TILE_CLASS`〈index 15。#1298 で未割当のまま残っていた枠を割当〉function constant・`TileClassRegion`）を opt-in で追加し、端あり形状込みで bit 同一を自己検証（`MetalGemm::new_with_tile_class`）。Linux 実行可能な被覆・互いに素の網羅テスト・証跡テストは green。実機〈M4 Max〉での T1〜T3・T6（全候補×N=512〜4096・端あり形状・`dispatch_auto`・既定値）は全 pass・既存 `#[ignore]` 群も非後退（T4/T5〈転置・`FragLoadConfig` 合成〉は時間制約により未実施）。**性能実測・採否は #1328 で完了（組み込み不可・REJECT。`docs/perf/metal-gemm-n4096-kernel-gap.md` §12 参照）**（イシュー #1327）
    │   ├── logs/metal-gemm-e6-tile-class-1327/ # 上記（#1327 機構実装）の実行ログ・env_info（内部ホスト名は含めない。イシュー #1327）
    │   ├── logs/metal-gemm-e6-tile-class-ab-1328/ # E6 性能実測・採否判断（候補 0/4/5/8 および本番選択構成の 5 プロセス起動計測）の実行ログ・env_info・aggregate.md（内部ホスト名は含めない。イシュー #1328）
    │   ├── logs/metal-gemm-mpp-tensor-1326/ # Metal 4 `tensor<>`＋MPP `matmul2d` 可用性調査（`backend-metal-mpp-tensor-decision.md`）の compile probe／parity smoke／5 プロセス起動 kernel_gpu A/B の実行ログ・env_info・aggregate.md（内部ホスト名は含めない。イシュー #1326）
    │   ├── metal-gemm-hfrag-candidate.md # half フラグメント（`simdgroup_half8x8`）／f32 累算候補カーネル `gemm_simdgroup_tiled_hfrag`（`gemm.metal` 末尾。E9・親 #1368）の追加・REQ-2 形状別判定方式に基づく全形状 × 転置 4 パターンの parity 実測（正しさゲートは丸め済み入力参照で全 PASS・丸めなし参照は設計上全形状で fail_count>0）・本番非結線（`docs/cuda-tf32-optin-api-decision.md` 同型の opt-in API 設計が前提）。M4 Max 実機実測（イシュー #1369）。§9 で #1370 の純カーネル時間 5 回中央値比較（S/A/B 系列）・採否判断を追記: N=4096 のみ本番選択構成比 約 10〜12% 高速（ADOPT-as-opt-in-candidate）だが同一タイル比較では hfrag が f32 より一貫して遅く、この速さは half MMA 自体の優位ではなく hfrag の SMEM 使用量が f32 の半分であることに由来する仮説段階の間接効果と推測される（A/B 系列は SMEM 使用量のみを対照した比較ではないため断定はできない。`CANDIDATES[9]` f32 版自体は実行可能で REJECT〈本番選択構成比 4.5〜7.6 倍遅い〉のため viability 拡大が実証されたわけではない）ため、無条件の opt-in 候補前進は推奨しないと結論（N=512 は REJECT・N=1024/2048 は共有負荷下で undetermined。M4 Max 実機実測2026-09-07）
    │   ├── logs/metal-gemm-hfrag-parity-1369/ # 上記（#1369 parity）の実行ログ・env_info（内部ホスト名は含めない。イシュー #1369）
    │   ├── logs/metal-gemm-hfrag-kernel-gpu-ab-1370/ # #1370 の S/A/B 系列 5 プロセス起動生ログ・`aggregate.md`（抽出コマンド・詳細表・妥当性帯突合）・env_info・uptime・pmset（内部ホスト名は含めない。イシュー #1370）
    │   ├── logs/metal-gemm-e2e4-select-closure-1304/ # E2〜E4 が REJECT 済みにつき組み込み対象なしと確定した記録・本番選択構成（`select_for_device`）× 全形状 × NN/NT/TN/TT parity・既存 bit 一致テスト群の実行ログ・`aggregate.md`・env_info（内部ホスト名は含めない。イシュー #1304）
    │   ├── cuda-tensor-core-tolerance-tf32x3-gb10.md # 3×TF32（split-single 法）の誤差分布（対 f32fma／対 f64）・スケールスイープ・純カーネル時間の GB10 実測記録（`docs/cuda-tf32x3-split-single-decision.md` §8 の引き継ぎ先）。`wmma_tolerance_probe.rs` の `--routes mma` 拡張（f32 SIMT／TF32 mma.sync／3×TF32 の 3 節・17 列表〈`ref`＝f32fma〈REQ-2 判定行〉／f64〈診断行〉〉・既定 `wmma` 出力は byte 単位で不変〈AC-2〉）・`gemm_tf32x3_kernel_time_bench`（純カーネル時間比較）を実装し GB10 実機実測を完了（2026-09-07）。P1（`#[ignore]` テスト 2 件・REQ-2 厳密ゼロ fail 判定）が FAIL（512×512×512・4096³ とも複合判定 FAIL）につき§11 の 3 択（推奨／条件付き推奨／非推奨）は未選択のまま baseline 提案値（未承認）を記録・ユーザー判断へ回す。P2（対 f64 精度）は K 支配的形状で部分的に不成立・P3（スケール依存）は 3 経路とも成立・P4（純カーネル時間）は 5 形状すべてで `mma_tf32x3 / f32_simt` < 1.0（0.721〜0.930 倍）。イシュー #1356。#1356 のユーザー判断で非推奨・baseline 不承認を確定（2026-09-08）
    │   ├── logs/metal-gemm-select-closure-framework-compare-1306/ # 候補表更新後の framework-compare gemm metal 結線前後 A/B（before=`fandhe-ai =0.7.0` registry・after=HEAD path patch。全 8 セル非後退・checksum 完全一致）の `run_ab_gemm_metal.sh` 実行ログ・`compare_gemm_ab.md`・env_info（uptime・load average 推移含む）・`diff_b2a5fcb_f396784_backend_metal_src.txt`（内部ホスト名は含めない。イシュー #1306）
    │   ├── logs/cuda-gemm-tf32x3-1356/ # 3×TF32 誤差分布（`--routes mma`。2 回実行）・`#[ignore]` テスト実行ログ（2 件 FAIL）・純カーネル時間 5 回プロセス起動 CSV・集計スクリプト（Python3 標準ライブラリのみ）・env_info（内部ホスト名は含めない。イシュー #1356）
    │   ├── cuda-host-view-staging-readout.md # `MemoryOps::with_host_view`（イシュー #1335）の CUDA 実装を追加し、形状ごとに再利用するホストステージングバッファ（`crate::host_staging::HostStagingCache`。take/put 方式・世代検査つき）へ `memcpy_dtoh` 1 回で D2H して借用を返す設計（`Pageable`／`Pinned` 2 種・unsafe 1 箇所〈`Pinned` 確保のみ〉・当初の既定は unsafe 経路を通さない `Pageable`）。GPU 非依存単体テスト・`#[ignore]` 実機テスト（bit 同一・キャッシュ再利用・`release_host_staging`・`PooledMemory` 透過・managed 配置・`CudaMemory::new_with_host_staging_kind` によるキャッシュ経由 `Pinned` 公平比較）を整備し、GB10 実機実測（2026-09-08）で D2H＋読み出し時間の before/after 5 回計測中央値を実行: 当初は計測クロージャの戻り値を `black_box` で保護する是正（codex-review P1・Cursor Bugbot 指摘。§5.3）前のバイナリで参考値を得たに留まっていたが、**イシュー #1438（2026-09-08）で是正後のバイナリによる再計測を完了**し §5.2 を確定値へ更新: glibc mmap 閾値（32 MiB）超の N=4096（64 MiB）で `before` 比約 7.9 倍改善に加え、N=1024/2048（閾値未満）も約 1.4〜1.6 倍の明確な改善を示した（is-optimized-away の懸念は解消）。ゲート C（情報のみ。キャッシュ経由 `Pinned` が全 N で `Pageable` をさらに上回る傾向）も確定。**イシュー #1478（2026-09-09 ユーザー承認）で `HOST_STAGING_KIND` の既定を `Pinned` へ切替済み**（unsafe 経路の既定化について実装フェーズで機械的自己監査を実施済み。正式な security-auditor 承認は PR レビューで行う。GB10 実機再計測は §8）
    │   ├── logs/cuda-host-view-staging-1336/ # 上記の GB10 実機実測（ゲート A 7/7 pass・A/B 5 プロセス起動生ログ・`aggregate.py`／`aggregate.md`）の実行ログ・env_info（内部ホスト名は含めない。イシュー #1336。#1438 で計測保護是正後の再計測値へ更新済み）
    │   ├── logs/cuda-host-staging-pinned-default-1478/ # `HOST_STAGING_KIND` の既定 `Pinned` 切替（#1478）の GB10 実機実測（ゲート A 8/8 pass・ゲート B 5 プロセス起動生ログ・`aggregate.py`／`aggregate.md`・ゲート C 縮小スコープ実測〈`gate-c-sanity.md`〉）の実行ログ・env_info（内部ホスト名は含めない。イシュー #1478）
    │   ├── cuda-h2d-pinned-staging.md # D2H 側（`HostStagingCache`。#1336/#1478）に対称な H2D 側 pinned staging（`crate::host_staging::{set_pinned_h2d_enabled, pinned_h2d_enabled}`・`H2dStagingCache`〈同一 numel 複数エントリ保持〉。既存 `unsafe` 1 箇所〈`HostStaging::alloc` の `Pinned` 分岐〉を再利用し新規 `unsafe` は追加しない）を既定 OFF・明示 opt-in で追加。`memory.rs::CudaMemory::{upload_inner, upload_into}`・`gemm.rs::CudaGemm::upload_h2d_new`（f32 の各 `run_*`／`launch_*` 系）・`ops.rs` の NT 転置分岐へ結線（TF32 Tensor Core 経路〈`run_wmma_tf32` 系〉は f32 系カーネルのため入力型上 `upload_h2d_new` を経由し対象内・f16 Tensor Core 経路〈`gemm_mma.rs` の `run_f16` 系・`run_f16_kernel`〉・elementwise 等は対象外）。facade `set_cuda_pinned_h2d_enabled`／`cuda_pinned_h2d_enabled`。GPU 非依存単体テスト 10 件（フラグ既定値 drift ガード・`H2dStagingCache` の take/put・世代不一致破棄・cap 超過破棄・poison 回復）は全 pass。実機 `#[ignore]` テスト（`tests/pinned_h2d_real_device.rs`。`PinnedHostSlice` 経由の連続 upload・部分更新・解放後の再確保の bit 単位検証。PR #1678 の codex-review 指摘対応で追加）まで整備済み。事前登録判定規則（ゲート A・Layer A 非後退・Layer B 改善根拠）を固定したが、本エージェント実行環境に DGX Spark GB10 実機への到達手段がなく実測は未実施のまま記入欄を残す（verdict=undetermined。既定 OFF 維持。イシュー #1585）。I3 の再確認は `docs/backend-cuda-async-execution-design.md` §3／§4／§10／§11 に追補済み
    │   ├── cpu-matmul-fixed-cost-impl.md # `docs/cpu-matmul-fixed-cost-design.md`（#1294）§3.C 案 1a の実装記録。`CpuBackendOps::gemm` の出力確保を rayon 並列ゼロ書き込みへ切り替える `zeroed_output`／`zeroed_output_with_threshold`（`crates/backend-cpu/src/ops.rs`）を実装し bit 完全一致回帰（クレート内・統合〈`gemm_output_alloc_bit_exact.rs`〉・facade 経由〈`tape_matmul_cpu_bit_exact.rs`〉）を追加（イシュー #1299）。#1301 で DGX Spark GB10・Apple M4 Max 両実機の on/off 5 回独立プロセス実測（Layer A/B）を実施し、DGX N=2048 の `alloc_c` 約 48% 削減・`ops_gemm` 合成 0.9972 倍・両実機全対照セル非後退を確認していったん本番既定しきい値 `GEMM_OUTPUT_PARALLEL_ZERO_MIN_ELEMS` を `2 << 20`（有効化・cfg gating なし）へ更新したが、事前宣言した candle 比非後退規則を計測後に緩和した基準だけで満たすとした点を PR #1448 の codex-review が指摘したため `usize::MAX`（無効化）へ差し戻し・§2・§6 を追記。#1481 独立再計測 REJECT（§6 追記）・#1482 で `usize::MAX` 確定既定化（§2 更新）
    │   ├── logs/cpu-matmul-fixed-cost-1299/ # 上記の M4 Max スモーク実測（before/after 各 5 プロセス起動）の生ログ・env_info（内部ホスト名は含めない。イシュー #1299）
    │   ├── logs/cpu-matmul-fixed-cost-1301/ # 出力並列ゼロ埋め（#1299）on/off 両実機実測（Layer A/B・DGX は専有確認後の clean 系列を正式値とする）の生ログ・env_info・`aggregate_layer_b.py`・`on-arm.patch`（内部ホスト名は含めない。イシュー #1301）
    │   ├── logs/cpu-matmul-fixed-cost-remeasure-1481/ # 出力並列ゼロ埋め（#1299）on/off の改定版規則 4（`docs/perf/cpu-gemm-candle-gate-remeasurement.md` §20.1a）による独立再計測（#1301 とは別セッション。Layer A/B・両実機 5 回独立プロセス起動）の生ログ・env_info・`judge_rules.py`（規則 1〜5 の機械判定・`--self-test` 付き）・`judge.md`・`aggregate_layer_b.py`・`on-arm.patch`（内部ホスト名は含めない。verdict=REJECT 確定。イシュー #1481）
    │   ├── cuda-host-view-readout-small-shape-regression.md # `host-view-readout` feature（#1335/#1336/#1337）有効時の CUDA reuse N=1024/2048 後退（15.04 倍・1.20 倍）を D2H 読み出し方式別（`clone_dtoh`＋`to_vec` 複製／借用 keep-alive／借用＋ダミー確保-解放／事前タッチ済み再利用宛先の 4 腕。`readout_regression_diag_tests_1436.rs`）にフェーズ分解し診断。増分は `matmul` 区間（`readback`＝`clone_dtoh`＋`synchronize`）に完全集中し `to_tensor`／`host_copy`／`checksum` は不変（交絡なしの確定観測。H2〜H5 棄却）。機構は glibc 動的 mmap 閾値適応の有無が有力仮説（`MALLOC_MMAP_THRESHOLD_` 固定による確証実験で裏付けあり）だが、Layer B は 4 腕を同一プロセス内で逐次実行しており先行腕の free／`keep_alive` 一括解放が後続腕のアロケータ状態へ引き継がれる交絡があるため、「on 腕固有の free 欠如が原因」という H1 の主張自体は腕単体・プロセス分離計測（入口は用意済み・本イシューでは未実行）による独立検証ができておらず仮説にとどまる（PR #1442 codex-review 指摘・§0/§8/§11）。N=2048 は bimodal（3 回中 2 回 slow・1 回 fast）を実測。N=4096 改善は D2H 自体でなく 2 回目のホストコピー省略に由来すると特定。イシュー #1436・親 #1435。**#1437 で是正完了**（§13）: `memory::readback` の宛先確保方式に `ReadbackDest::PretouchedFresh`（反復ごとに非ゼロ sentinel で事前タッチ済みの宛先を新規確保して `memcpy_dtoh`。`ReadbackSentinel` トレイトで f32/f16 の型ごと非ゼロ値を強制・`Fresh` と bit 完全一致）を追加し既定へ結線。Layer B 分離計測（`PretouchedFreshDest` 腕。単一腕プロセス分離実行で H1 の交絡問題も解消）で N=1024（37.15→2.04 ms）・N=2048（11.32→7.34 ms）・N=4096（29.26→30.37 ms）を確認したうえで、Layer A（framework-compare）ゲート判定を実施: 事前宣言 Gate 1（全 N で `on@after/off@base ≤ 1.00`）は 0.637〜0.897 倍で全 N 通過・Gate 2（`off@after/off@base ≤ 1.03`。受け入れ条件自体には含まれない自己宣言の非後退目安）は N=1024/2048 でわずかに超過（1.038・1.047）——`readback()` の既定 `ReadbackDest` が feature 有効・無効を問わず無条件に `PretouchedFresh` へ切り替わるため `off@after` にも事前フィル費用が一律で乗ることが機構的な説明として整合し（当初の「後退要因が存在しない」という断定は撤回）、超過幅は Gate 1 の改善幅より小さいため ADOPT 判断自体は変更しない（§13.3 再評価）。実機 `#[ignore]` bit 一致テスト（`readback_pretouched_bit_match_1437.rs`）・既存 `#[ignore]` 群の非後退（既知の pre-existing FAIL を baseline 個別実行で再確認）も完了。GB10 実機実測（2026-09-08）。**#1438 で feature ゲート撤去・既定経路化を完了**（旧 `host-view-readout` cargo feature を撤去し bench-fandhe の既定経路化・3 バックエンド × 両実機の before/after 実測で判定対象の reuse 全セル非後退・checksum 完全一致を確認〈CPU fresh 参考行は M4 Max N=512/1024 が僅かに後退方向・ノイズかは未確定〉。詳細は `docs/perf/{cuda,metal,cpu}-gemm-candle-gate-remeasurement.md` の該当節）
    │   ├── logs/cuda-host-view-readout-regression-1436/ # 上記の Layer A（bench-fandhe off/on `--phases` jsonl）・Layer B（4 腕診断テスト実行ログ・`MALLOC_MMAP_THRESHOLD_` 固定確証実験）・env_info（内部ホスト名は含めない。イシュー #1436）
    │   ├── logs/cuda-host-view-readout-fix-1437/ # 上記 #1437 是正の Layer B（`PretouchedFreshDest` 腕を含む単一腕プロセス分離実行）・Layer A（`off@base`／`off@after`／`on@after` 3 系列 JSONL・manifest）の生ログ・env_info（内部ホスト名は含めない。イシュー #1437）
    │   └── device-checksum-readback-ab.md # checksum（全要素和）をデバイス側 f64 reduction で求め読み戻しを 8 バイトにする経路を bench-fandhe（`Var::matmul_checksum`・`device-checksum` cargo feature）と bench-candle（`sum_all().to_dtype(F64)`。Metal は F64 reduction 非対応のため f32 のまま）へ実装。`tensor-core::BackendOps::gemm_checksum`／`autodiff::Var::matmul_checksum` の公開 API 追加・`backend-cpu` 実装（`gemm` と bit 同一・checksum はホスト f64 逐次和と bit 一致）・python 集計ツール群（summarize.py 等）の `device_checksum` 除外を完了。`backend-cuda`／`backend-metal` の GPU 側 reduction カーネル実装は本イシューのスコープ外（デフォルト `Unsupported` のまま）で後続イシューへ引き継ぎ・実機 A/B 実測は未実施のまま記入欄を残す（イシュー #1339）
    ├── performance-targets.md # REQ-8 段階的下限の全バックエンド横断一覧（TASK-8.4・#159）
    ├── public-api-design.md            # compat API 層の公開 API 設計（REQ-9）
    ├── real-hardware-verification-env.md # 実機検証環境（Mac Metal / DGX Spark CUDA。実ホスト名はローカル管理外ファイル参照）の接続・転送・計測手順（#408・#461）
    ├── real-hardware-verification-env.local.md.example # 上記の実値（内部ホスト名等）を書くローカル用テンプレート（#461。実体は .gitignore 対象）
    ├── self-repair-candidate-isolation.md # 候補実行の OS レベル縦深防御の調査結果・採否判断（#414）
    ├── self-repair-revalidation-plan.md # TASK-3.3a 自己修復ループ再実証の実証計画・題材選定（#140）
    ├── spec-proposal-req2-candle-parity-tolerance.md # REQ-2 統一複合判定へのスケール付き絶対誤差救済項（線形 K 形式・案 1′〈√K 形式〉との相違を明記）の追加／比較対象側（candle 等）fail の「判定不能」規定を fandhe-ai-spec へ提案する draft。spec リポへそのまま起票可能な本文案を含むが、実起票はイシュー #1241 でのユーザー承認後に限る（`docs/spec/` は不変・未起票。イシュー #1240。**#1241 承認を受け 2026-09-08 に (b) 形式で Fandhe-AI/fandhe-ai-spec#64 として起票済み**）
    └── spec/                # 正本 submodule（fandhe-ai-spec。編集禁止）
        ├── 04-requirements.md  # REQ-1〜14
        ├── 05-tasks.md         # TASK 一覧（4h 粒度）
        ├── 06-roadmap.md       # M0〜M5・全 51 タスク
        └── 03-poc/             # PoC 実測（v2 系は poc-v2-*）
```

## 委譲方針（必読）

main はコンテキスト消費を抑えるため判断と統合に専念し、調査・実装・テスト・レビューは subagent へ委譲する。詳細は `.claude/rules/delegation.md`（調査・設計）・`delegation-impl.md`（作成・編集）を参照。

### model 配分

| 用途 | model |
|------|-------|
| 複雑な横断判断・アーキテクチャ設計 | opus または fable（fable は特に大規模設計・横断判断の最上位 tier） |
| 調査・生成・実装・レビュー | sonnet |
| 機械的集計・lint・ドキュメント更新 | haiku |

## Sub-agents

| カテゴリ | subagent_type | 担当 | model |
|---------|---------------|------|-------|
| research | explorer | コードベース・docs/spec 横断調査（読み取り専用） | sonnet |
| research | reference-researcher | cudarc/CUDA・objc2/Metal・safetensors/ONNX 等の外部仕様調査 | sonnet |
| implement | core-builder | `tensor-core`・`autodiff`・workspace 骨格・`facade`（composition root・compat API 層） | sonnet |
| implement | backend-builder | `backend-cpu`・`backend-cuda`・`backend-metal`・数値一致回帰テスト | sonnet |
| implement | interop-builder | `onnx-interop`（safetensors / prost 自前取り込み） | sonnet |
| implement | runtime-builder | `guardrail`・`self-repair`・`bench-harness` | sonnet |
| testing | test-runner | テスト実行・追加・失敗解析（実機依存は `#[ignore]` 分離） | sonnet |
| testing | bench-runner | ベンチ計測・性能回帰検出（5 回計測中央値・読み取り専用） | sonnet |
| quality | reviewer | コードレビュー（spec 突合・読み取り専用） | sonnet |
| quality | security-auditor | OWASP Top 10・unsafe・ライセンス監査（読み取り専用） | sonnet |
| quality | linter | fmt / clippy / frontmatter lint の機械的実行 | haiku |
| docs | docs-writer | CLAUDE.md・README・license-matrix 等の更新 | haiku |

## Rules

| ファイル | 内容 |
|---------|------|
| `.claude/rules/delegation.md` | 調査・設計フェーズの委譲原則・パスベース切り替え |
| `.claude/rules/delegation-impl.md` | 作成・編集フェーズの委譲マッピング・実装フロー標準 |
| `.claude/rules/coding-rust.md` | 完全自作コア方針・cfg ベースバックエンド・FMA 契約統一・品質基準 |
| `.claude/rules/deps-policy.md` | 許容依存 8 区分・`=x.y.z` 完全固定・禁止リスト・ライセンス要件 |
| `.claude/rules/ci.md` | **CI は GitHub ホステッド（`ubuntu-latest`）既定**（例外は codex-review の codex 実行ジョブのみ）・fork PR 対策・timeout 必須・SHA 固定・fail-closed 集約 |
| `.claude/rules/security.md` | OWASP Top 10・秘密情報混入防止・自己修復ループのガードレール |
| `.claude/rules/japanese-style.md` | 日本語出力スタイル |
| `.claude/rules/conventional-commits.md` | Conventional Commits 詳細規約（`--no-verify` 禁止） |
| `.claude/rules/code-comment-style.md` | コメント規約（役割・責務・呼び出し文脈・spec 根拠を埋め込む） |
| `.claude/rules/out-of-scope-tracking.md` | 実装対象外の追跡規約（スコープ外事項を放置しない） |

## Current Skills

`npx skills add` で導入済み（`skills-lock.json` 管理。更新は update-external.yml が自動追従）。

- **Git/GitHub 運用**: create-commit・create-pr・create-issue・create-issue-tree・update-issue-tree
- **実装フロー**: create-plan・implement-issue・implement-issue-tree・implement-review・implement-review-pr
- **ドキュメント**: update-docs・comment-code
- **スキル管理**: init-claude・update-claude・contribute-skill・sync-skills-lock
- **技術リファレンス**: rust・nvidia-cuda・apple-silicon・amd-rocm・lefthook・editorconfig・commitlint・github-docs

## Conventions

- 日本語でやりとり・報告・コミット・PR を書く（`japanese-style.md`）
- Conventional Commits 厳守・`--no-verify` 禁止（`conventional-commits.md`）
- 依存の追加・更新、ガードレール閾値・テスト許容誤差の変更はユーザー承認必須
- `docs/spec/`（正本 submodule）は編集しない。仕様変更は spec リポ側で対応する
- implement-issue は計画のユーザー承認後に実装する
- スコープ外事項は `out-of-scope-tracking.md` の規約に沿って Issue で追跡する

## hooks（settings.json）

- **SessionStart**: 日本語・委譲・完全自作コア・CI は GitHub ホステッド・Conventional Commits のリマインダーを表示する
- **PostToolUse**（Edit|Write）: `.rs` 編集時に rustfmt を自動適用する（Cargo.toml の edition を検出。未追加時は 2021 フォールバック）
